# Protocol Notes

## 术语与命名

- 当前仓库把这套历史兼容生态统一注明为 `图文卡 / 通视 / 分析家通用`。
- 这个命名主要用来指代“分析家、飞狐交易师、大智慧等旧软件可对接的通用接口生态”，方便对照 `StockDrv.dll / Stock.dll / Stock64.dll`、官方示例和抓包样本。
- 但这里必须和“当前线上字节流形态”分开看：
  - 现在抓到的真实会话并不是直接裸跑老式公开 `图文卡` 数据帧。
  - 当前线上链路已经明确出现 `网络包 / penc / ZSTD字典 / 下载文件` 这些网际风自有外壳与后续层。
  - 所以“图文卡/通视/分析家通用”在本仓库里首先是术语标签与兼容背景，不是对当前 TCP 负载封装方式的结论。

- 本仓库的主判断仍以两类资料为准：
  - 项目内已纳入的官方/历史参考目录 `./netzip_api_bin/NetzipAPI`
  - 当前仓库实际抓到并可重组验证的会话样本

- 网上可以找到一些第三方镜像/旧博客，会把这套接口直接称为“网际风通视接口”或“分析家通用接口”。
  - 这些材料可以帮助做术语校对，或提供 `下载文件 / FILE_* / RCV_*` 这种结构线索。
  - 但它们不是当前仓库的主证据来源，不能替代官方参考目录和真实抓包。

### 网上可见的非官方对照线索

- 第三方镜像里的“网际风通视接口”规范，能看到一组和旧生态高度一致的消息常量与文件头说明：
  - `RCV_REPORT = 0x3f001234`
  - `RCV_FILEDATA = 0x3f001235`
  - `RCV_FILE_HEADEx` 中包含 `m_dwAttrib / m_dwLen / m_dwSerialNo / m_szFileName`
  - 这对我们当前看到的“`下载文件` + 文件名 + 序列号/长度语义”是有帮助的校对线索

- 第三方镜像里的“网际风客户端版本更新历史”还提到：
  - 网际风在兼容 `通视协议` 的同时，后续又增加了自身扩展
  - 其中包括 `RI_DATAExtSUPPORT1 / RI_DATAExtSUPPORT2`
  - 以及 `FILE_HISTORY_EX / FILE_MINUTE_EX / FILE_POWER_EX / FILE_5MINUTE_EX / FILE_1MINUTE_EX`
  - 这和我们现在看到的“兼容外层 + 网际风自有扩展/后续壳”判断方向一致

- 因此，网上材料目前最有价值的用途是：
  - 校对“`通视/分析家通用` 这条命名确实存在”
  - 校对 `RCV_FILEDATA / FILE_* / 驱动扩展能力位` 这套高层结构术语
  - 辅助解释为什么 `下载文件` 这类负载在生态层面是合理的

- 但这些网上资料仍然不能直接解决当前最难的部分：
  - `7100` 登录后的 `penc / ZSTD字典 / Tdx_Encrypt` 具体编码链
  - 当前线上 `网络包` 私有外层和旧 `通视/图文卡` 高层语义之间的精确映射
  - 所以它们只能作为辅证，不能替代当前仓库内的抓包与样本解析

## Windows 现场补证与分层视图（2026-03-29 / 2026-03-30）

### 分层结论

- 当前最稳妥的运行链要分 3 层描述：
  - 上层 API 语义层：`Start / Ask / Stop`
  - 本地桥接层：`127.0.0.1:2000`（`2001` 在现场样本中处于预留监听）
  - 上游远端链路：`6100 / 7100 / 7709 / 7719 / 7708 / 14017`
- 在 Windows 现场中，`FoxTrader.exe` 与 `Stock.dll` 都会连接本地 `127.0.0.1:2000`，再由 `网际风.exe` 对接上游服务器。
- 所以本文里凡是“请求串”相关描述，都应优先理解成 `Ask(...)` 的上层输入语义，不应直接等同于上游远端 TCP 负载。

### 已确认事实（来自 windows_debug 现场证据）

- `Ask(...)` 的明文请求串属于上层 API 输入，不是“直接写到 6100 socket 的裸文本”。
- 当前现场运行链已经坐实为：
  - `FoxTrader.exe / Stock.dll -> 127.0.0.1:2000 -> 网际风.exe -> 上游 6100 / 7100 / 7709`
- `网际风.exe` 在现场监听：
  - `127.0.0.1:2000`
  - `127.0.0.1:2001`
  - `127.0.0.1:5188`
  - `127.0.0.1:22223`
- 其中：
  - `2000` 是当前 `FoxTrader` 主链入口
  - `2001` 在现场窗口中只看到监听，未见主链使用
  - `5188 / 22223` 更像其它兼容接口，不是本次 `FoxTrader` 主链
- `Ask(...)` 回包按当前现场证据和库层抽象，已经能稳定分 3 类：
  - `ret=0` 的“同步空返回但请求已受理”，后续结果走 callback
  - 文本/JSON 控制流（例如登录阶段 `ret=0` 后异步提示、初始化 `ret=70` 同步提示）
  - `OEM_DATA_HEAD(200) + payload` 的结构化二进制回包（如实时、K 线）
- 当前仓库已经把这套模型落成 `src/stock_message.rs`，统一保留：
  - 到达通道：`SyncReturn / Callback`
  - 负载类型：`Empty / Text / Packet / UnknownBinary`
  - 文本键：`text_request / text_data`
  - OEM 头字段：`packet_type / packet_label / packet_name / packet_count / packet_len / packet_flag / packet_ask_id / packet_power / packet_oem_ver`
- 现场还确认：
  - `Start ret=1`
  - 首条回调文本是 `连接 网际风.exe 成功`
  - 当前真实安装目录主用的是 `x86 Stock.dll`，没有 `Stock64.dll`
- callback `form` 当前至少已坐实：
  - `提示信息`
  - `消息`
  - `股票数据`
  - `错误`
- `Ask(...)` 请求串里的 `编号` 不能直接当成回包 `ask_id`：
  - 现场样本里 `编号=101/102/103`
  - 同步结构化回包分别是 `ask_id=4/5/6`
- `127.0.0.1:2000` 不是明文请求口：
  - 裸 `UTF-8/UTF-16LE` 请求串直发都没有有效回包
  - Frida 抓到的本地 payload 已出现 `网络包` 前缀、`penc`、`hypenc`
- 本地 `recv len=10240` 不是协议包长，而是大对象搬运块大小：
  - 单个 `recv` 内可以串多个完整小包（例如 `600 = 288 + 312`、`1188 = 288 + 368 + 532`、`1264 = 340 + 288 + 304 + 332`）
  - 后续解析必须按包头与长度字段（`declA`）重组，不能按 `recv` 边界切包
- 当前仓库还新增了 `src/local_2000_vs_auth7100.rs` 作为跨层保守对位器，当前样本已经坐实：
  - 本地 `2000` 完整小包与远端 `7100` 前缀都复用 `field40 = 2`
  - 两边都复用重复长度字段，且当前样本里 `declared == duplicate`
  - 两边都出现 `fixed_overhead_len_hint = 28`
  - 本地 `2000` 小包额外暴露 `packet_len_declared - payload_len_hint = 68`、`attr_flags_hint = 9`、`penc@60`
  - 远端 `7100` 这边则已经分化出 `compressed_zstd / compressed_zstd_dict / download_file_record` 三类布局

### 关系边界（避免过度结论）

- 本地 `2000` 和上游 `6100/7100` 都出现 `网络包/penc` 家族特征，但当前还不能据此断言二者“字节级完全同一协议”。
- 当前更稳妥的表述是：
  - 二者至少共享一组前缀层不变量
  - 但本地 `2000` 目前看到的是 `header_len=68 / attr=9 / penc@60` 这一类完整小包
  - 远端 `7100` 同时覆盖 `attr=0/9`、`object_type=1/4` 和多种压缩/下载布局
  - 按 `field40 / attr / fixed_overhead / header_len / outer_wrapper` 这 5 组导出信号做精确桥接时，当前样本只命中 `download_file_record / object_type=1`
  - 同一组精确桥接不会命中 `compressed_zstd / compressed_zstd_dict`，所以当前更像“本地完整小包前缀族和远端下载文件前缀族同族”，而不是直接落在 zstd 壳包族上
  - 这条结论现在不只覆盖本地 `complete-small`，也覆盖初始化阶段抓到的本地大对象起始包；两者当前都命中同一组 `download_file_record` 桥接信号
  - 新增的 boundary delta 还把首个 `penc` 对到了同一位置：本地 `complete-small`、本地大对象起始包、远端 `1540 download_file_record` 当前都满足 `penc = payload boundary - 8`，而且首个 `penc` 的精确偏移都落在 `60`
  - 与之对应，本地 `hypenc` 当前贴着 `payload boundary`，但远端 `1540 download_file_record` 还没有看到 `hypenc`
  - 远端 `1540 download_file_record` 额外还能直接看到第二个 `penc@416`，所以第 19 阶段当前剩下的更深缺口已经收缩成“`hypenc@68` 和 `penc@416` 的结构含义”
  - 继续把后续 marker 量出来以后，当前形态已经更明确：远端第二个 `penc@416` 对应 `payload boundary + 348`，而本地大对象后续 `penc` 当前落在 `payload boundary + 2 / +190 / +53888`；也就是说“首个 `penc` 的桥接”已经坐实，但更深一层 payload 内壳目前还没有直接对齐
- 当前仓库里对 `6100/7100` 的结论主要来自上游会话抓包重组；对 `2000` 的结论主要来自 Windows 现场探针与 Frida 取证，两者应并行校对而不是互相替代。

### 首批跨层对位锚点（2026-03-30 深夜）

- 虽然还不能下“本地桥 = 上游 wire 完全同构”的结论，但当前已经有一组可以直接拿来做字段级对位的锚点：
  - 本地 `2000` 完整小包与上游 `7100` 前缀摘要里都稳定出现 `field40 = 2`
  - 本地 `2000` 完整小包与上游 `7100` 前缀摘要里都稳定出现 `field56 - field44 = 28`
  - 本地 `2000` 完整小包里 `penc` 的稳定偏移是 `60`
  - 上游 `7100` 侧已经把同一组差值保守抽象成 `fixed_overhead_len_hint = 28`
  - 现在远端 `1540` 下载文件包也已经直接量到 `penc_offsets = [60, 416]`
    - 其中首个 `penc@60` 与 `header_len=68` 对位后，恰好也是 `payload boundary - 8`
- 大对象/下载链上也已经出现一个值得优先核对的同值字段：
  - 本地 `2000` 第一批初始化大对象起始包稳定出现 `field52 = 9`
  - 上游 `7100` 的 `1540` 字节 `下载文件` 包也稳定出现 `field52 = 9`
  - 且这个 `9` 当前已经能和 `请求 / 来源 / 应答编号 / 名称 / 长度 / 原长度 / 压缩 / 数据 / 扩展` 这 `9` 个顶层可见子项对上
- 所以后续第 19 阶段应优先围绕这些锚点推进，而不是继续只做模糊的“同源/不同源”讨论：
  - `field44 / field52 / field56` 的跨层语义是否一致
  - 本地 `penc / hypenc` 与远端 `compressed_zstd / compressed_zstd_dict / download_file_record` 的对应关系
  - `field52 = 9` 在本地大对象和远端下载文件里是否都可解释为同一类“子项数 / attr 族”字段
  - `field48 = 0` 是否只是本地桥层保留位，而不是远端 `object_type_id`

## 综合总结（截至 2026-03-30）

- 当前已经能把这套链路稳定拆成 3 层：
  - 上层 `Start / Ask / Stop` API 语义层
  - 本地 `127.0.0.1:2000` 二进制桥协议层
  - 上游 `6100 / 7100 / 7709 / 7719 / 7708 / 14017` 会话层
- 当前最重要的修正是：
  - 不能再把 `Ask(...)` 的明文请求串直接当成“发送到 `6100/7100` 的 wire payload”
  - 不能再把本地 `recv len=10240` 误判成天然协议消息边界
  - 不能把“本地 `2000` 和上游 `6100/7100` 都出现 `网络包/penc`”直接写成“二者已经逐字节同构”
- 目前已经跑实的硬证据包括：
  - `FoxTrader.exe` 与现场 `Stock.dll` 都会连接本地 `127.0.0.1:2000`
  - `网际风.exe` 负责监听本地桥并继续对接上游 `6100 / 7100 / 7709`
  - `Ask(...)` 至少同时存在文本/JSON 控制流和 `OEM_DATA_HEAD + payload` 二进制流两类回包
  - 上游 `7100` 的 `auth_probe / auth_login / 下载 系统\\通达信股票服务器.ini / 后续 penc+字典壳` 已可稳定按样本矩阵输出
  - 上游 `7709` 的代码表同步与 `0x0547` post-login 阶段已经能在真实会话里定位
- 目前真正卡住的主缺口也已经收敛：
  - 本地 `2000` 还缺按包头 + `declA` 的正式重组器
  - `2000` 与上游 `6100/7100` 还缺壳字段级系统对位
  - `7100` 登录后的 `penc / ZSTD字典 / Tdx_Encrypt` 仍未还原成可逆处理链
  - `Ask(...)` 的文本控制流与结构化二进制流还没有在 Rust 侧统一成稳定抽象
- 因此后续主线已经明确：
  - 先补本地 `2000` 重组器
  - 再做 `2000 <-> 6100/7100` 壳层对位
  - 同时继续推进 `7100` 登录后壳层解码
  - 最后把这些结果回灌到统一的 `Ask` 上层抽象和 Web 调试入口

## 本地 `127.0.0.1:2000` 重组器（2026-03-30）

- Rust 侧现在已经有了正式的本地桥日志解析器：
  - 模块：`src/local_2000.rs`
  - HTTP 入口：`POST /api/debug/local-2000-log-scan`
- 当前实现已经覆盖这些现场规则：
  - 按 `51 7f dc 7e 05 53 00 00` 识别外层头，再读 `declA / declB`
  - 按 `declA` 连续消费，不再按单次 `recv` 边界切包
  - 支持单次 `recv` 内多包串接
  - 支持大对象跨多个 `10240` 搬运块续传
  - 支持 `[recv]` / `[recv-large]`
  - 支持新版 `recv-large index=...`
  - 支持 ANSI 颜色与 `NNN:` 行号前缀摘录
  - 支持“下一个对象头出现在 continuation chunk 中间”时，先吞掉前缀 continuation 再切到下一对象
- 当前已经在 Rust 回归里锁住 `windows_debug` 的 `v11` 样本事实：
  - `recv entries = 1940`
  - `recv entries with multiple headers = 5`
  - `complete small packets = 19`
  - `aligned next-header links = 12`
  - 大对象首块 `declA = 741918`
- 当前重组器输出的重点是：
  - 每个 `recv` 的头命中、长度场、`penc/hypenc` 偏移
  - 完整小包分布
  - 大对象跨块重组结果
  - UTF-16 证券代码步长提示
  - 在“已完整捕获 `OEM_DATA_HEAD` 声明总长”的前提下，安全尝试补做 `代码表/除权` 内层摘要
- 当前仍保守的边界：
  - 对未完整捕获的大对象，不再冒险直接喂 `unsafe` OEM 解析
  - 所以不完整大对象目前只输出外层重组、代码步长与可见 marker，总量恢复仍依赖更长采样窗口或已有 `recv_large_*.bin`

## 当前已确认

- 本地上层调用规范是明文请求串，例如：

```text
股票数据?请求=登录&模块=认证&账号=&密码=&自动升级=稳定版&版本=20221120&等待=10000&编号=0
```

- 这些请求串来自本地文档：
  - `./netzip_api_bin/NetzipAPI/股票接口调用规范.txt`
  - `./netzip_api_bin/NetzipAPI/StockC++/OemStock.h`

- 上面的请求串是 `Ask(...)` 的上层 API 输入语义，不等于上游远端 wire payload。

- `Ask(...)` 的回包语义当前已确认至少两类：
  - 文本/JSON 控制流（提示信息、登录状态、初始化提示等）
  - 结构化二进制流（`OEM_DATA_HEAD` 固定 200 字节 + 业务 payload）

- 当前已确认会出现在认证/登录样本与配置中的上游节点包括：
  - `121.41.70.217:6100`
  - `121.41.70.217:7100`
  - `39.108.103.69:6100`
  - `39.108.103.69:7100`

- `3000/3001` 在当前探测下不开放；当前已验证会话主要落在 `6100` 的认证测速层和 `7100` 的认证登录层。

- Windows 抓包中出现的前缀 `51 7f dc 7e 05 53` 不是随机魔数，它正好是 UTF-16LE 的 `网络包`。

- 当前 Windows 样本里的首段 `74` 字节前缀，已经能稳定读出这些字段组合：
  - `网络包 ... 压缩 | ZSTD`
  - `网络包 ... 数据 | 认证`

- `pktmon` 抓到的同一条 TCP 包会重复 4 次，因此需要按 `src/dst/seq/ack/flags/payload` 去重。

- 当前 `pcapng/raw` 文件存在截断：
  - 例如首个客户端请求包，链路层 `orig_len = 473`
  - 但实际写入文件的 `incl_len = 128`
  - 留给 TCP payload 的只有 `74` 字节可见前缀
  - 所以当前样本足以逆向“信封头前缀”，但不足以还原完整请求体/压缩正文

- 当前已从会话级别确认的顺序，需要按“认证测速”和“认证登录”两阶段来读：
  - 认证测速阶段：
    - 小会话形态是 `419 -> 345/340`
    - 语义是 `认证 | 请求=测速`
    - 这一层当前既在旧样本里见过 `6100`，也在完整 sample pcap 的 `7100 auth_probe` 里见过
  - 认证登录阶段：
    - 当前完整 sample pcap 已稳定确认 `39.108.103.69:7100` 上的 `611/271/333 -> 443/1540/395`
    - 语义是 `认证 | 请求=登录 -> 数据 | 下载文件 -> 后续 penc / ZSTD字典 / Tdx_Encrypt` 壳
  - 因此不能再把后续 `611` 登录包简单记成“发往 `6100` 的下一个请求”

- 新的完整无截断抓包已经确认：
  - `pktmon start -c --pkt-size 0` 解决了 payload 截断
  - 但 `pktmon` 仍然会重复采到同一条 TCP 包
  - `capture_netzip_full.pcapng` 经过 `pcap_summary` 统计后：
    - `pcap-packets = 88`
    - `unique-packets = 20`
    - `duplicate-packets = 68`
    - `truncated = 0`

- 完整首包 `client_to_server_full.raw` 的结构现在已经能落到具体字节：
  - 文件总长 `3352` 字节，本质上是 `8 x 419` 字节完整请求包的拼接
  - 单个唯一请求包结构是：
    - 外层 `网络包` 前缀：`74` 字节
    - `ZSTD` 压缩正文起点：偏移 `0xa8` (`168`)
    - 单帧压缩长度：`249` 字节
    - 解压后长度：`434` 字节
  - 解压后的里层对象头是：
    - 对象名：`认证`
    - `tail = 请求 | 测速`

- 这个 `419` 字节客户端首包已经不再是“未知初始化请求”，而是认证服务器测速请求。
  当前已从解压正文中读出的字段有：
  - `请求 = 测速`
  - `账号 = 168`
  - `密码 = 168`
  - `分析软件 = 自定义`
  - `运营商名称 = 泉州移动`
  - `模块 = 股票客户端`
  - `Stock.exe_日期`
  - `Stock.exe_版本`
  - `操作系统`
  - `客户标识`

- 完整首个服务端回复 `server_to_client_full.raw` 也已经能解压：
  - 文件总长 `2760` 字节，本质上是 `8 x 345` 字节完整回复包的拼接
  - 其中包含 `2` 个唯一回复包，对应两个认证服务器返回的不同统计值
  - 单个回复包的压缩正文：
    - `ZSTD` 起点同样是偏移 `0xa8`
    - 压缩长度：`175` 字节
    - 解压后长度：`328` 字节
  - 解压后的里层对象头同样是：
    - 对象名：`认证`
    - `tail = 请求 | 测速`
  - 当前已读出的回复字段有：
    - `来源 = 认证服务器`
    - `应答编号`
    - `当前登录数`
    - `最大登录数`
    - `服务器编号`
    - `总带宽`
    - `现带宽`

- 现在已经不需要再依赖“手工切 raw”来分析多包会话：
  - `pcap_reassemble` 可以从 `pcap` 去重并重组真实 TCP 字节流
  - `stream_analyze` 可以在一整条流里自动切出多个 `网络包`
  - 这已经把 `6100` 的测速阶段和 `7100` 的登录阶段分开了

- `6100` 这组小包仍然是认证服务器测速，不是行情登录：
  - `192.168.3.38:2694 -> 39.108.103.69:6100` 的 `419` 字节包
    - 外层 `网络包`
    - 标准 `ZSTD` 压缩正文
    - 里层 `认证`
    - `tail = 请求 | 测速`
  - 对应 `345` 字节回复同样是标准 `ZSTD`
    - `来源 = 认证服务器`
    - `当前登录数 / 最大登录数 / 服务器编号 / 总带宽 / 现带宽`

- `7100` 这组包已经进入真正的认证登录阶段：
  - 客户端流 `192.168.3.38:2697 -> 39.108.103.69:7100`
    - 共 `3` 个 `网络包`：`611 / 271 / 333`
    - 其中首个 `611` 字节包是标准 `ZSTD`
    - 解压后里层对象为 `认证`
    - `tail = 请求 | 登录`
    - 当前可见字段：
      - `账号 = 168`
      - `密码 = 168`
      - `分析软件 = 自定义`
      - `运营商名称 = 泉州移动`
      - `模块 = 股票客户端`
      - `网际风.exe`
      - `Stock.dll`
      - `Stock.字典`
  - 服务端流 `39.108.103.69:7100 -> 192.168.3.38:2697`
    - 共 `3` 个 `网络包`：`443 / 1540 / 395`
    - 中间的 `1540` 字节包不是压缩包，而是明文 `数据 | 下载文件`
    - 其中直接出现：
      - `请求`
      - `系统\\通达信股票服务器.ini`
      - `来源 = 认证服务器`
      - `名称 = 系统\\通达信股票服务器.ini`
      - `压缩 = 无压缩`
      - `账号 = NetCardMac`
      - `密码 = l123321`
      - `主端口 = 7709`
      - `次端口 = 7712`
      - `客户ID = 1031`
      - `版本 = 1`
      - `F10序号 = 0`
      - `市场 = SH;SZ`
      - 多组 `7709` 行情服务器地址：
        - `124.70.183.173`
        - `124.71.163.106`
        - `180.101.48.175`
        - `120.195.71.160`
        - `122.96.107.241`
    - 当前仓库已经把这段 `1540` 字节包里的 `系统\\通达信股票服务器.ini` 直接解析成结构化结果：
      - 活跃节点 `5` 个
      - 注释掉的“通达信官方”节点 `5` 个
      - 旧连接面板现在会用这份样本填充“股票备用”服务器下拉框
    - 这条 `1540` 下载包当前还能稳定看到：
      - `penc_offsets = [60, 416]`
      - 其中首个 `penc@60 = payload boundary - 8`
    - 现在这条 `1540` 下载包还会额外暴露一个“顶层子项计数”派生结果：
      - `field_52 = 9`
      - 顶层可见 label 也正好是 `9` 个：
        - `请求`
        - `来源`
        - `应答编号`
        - `名称`
        - `长度`
        - `原长度`
        - `压缩`
        - `数据`
        - `扩展`
      - 所以当前只把它记成“下载文件顶层 child/item count 的强提示”，没有把 `field_52` 全局改名成 `child_count`
  - 当前 sample pcap 里，`7100` 会话矩阵也已经落成接口：
    - `GET /api/debug/auth-7100-flow-matrix`
    - `POST /api/debug/auth-7100-flow-matrix`
      - 请求体：`{ "path": "/abs/path/to/file.pcap" }`
      - 也可以直接给 `.pcapng`；服务端会调用系统 `tcpdump` 临时转换成 classic `pcap`
      - 还支持可选过滤：
        - `local_endpoint`
        - `session_role`
        - `source_endpoint`
        - `destination_endpoint`
    - 当前样本稳定归纳为 `2` 条完整会话：
      - `192.168.3.38:2695 <-> 39.108.103.69:7100`
        - `419 -> 345`
        - 角色 = `auth_probe`
      - `192.168.3.38:2697 <-> 39.108.103.69:7100`
        - `611/271/333 -> 443/1540/395`
        - 角色 = `auth_login`
    - 这条矩阵输出现在还带了前缀提示摘要，当前样本能稳定确认：
      - `field32 / field36` 是重复包长声明
      - `field40` 在已观测 `6100 / 7100` 样本里恒为 `2`
      - `field56 - field44` 在外层 `网络包`、内层 `认证`、以及 `数据|下载文件` 记录里都固定为 `28`
      - 因此代码里先按保守语义输出：
        - `packet_len_declared`
        - `packet_len_duplicate`
        - `field40_constant`
        - `payload_len_hint`
        - `attr_flags_hint`
        - `object_span_len_hint`
        - `fixed_overhead_len_hint`

- `7100` 里其余几包虽然 `tail` 仍写着 `压缩 | ZSTD`，但它们并不是标准裸 `ZSTD`：
  - 原始 UTF-16 字段会直接出现：
    - `压缩`
    - `ZSTD字典`
    - `长度`
    - `原长度`
    - `数据`
  - 原始 ASCII 字段会出现：
    - `penc`
    - `W[xQ`
    - `Tdx_Encrypt`（服务端 `395` 字节包）
  - 当前 `stream_analyze` 已能稳定打印出伪 `ZSTD` 头；例如：
    - `271` 字节客户端包：`28 b5 2f fd 20 82 c5 02 00 e4 03 0b ...`
    - `333` 字节客户端包：`28 b5 2f fd 60 ae 00 ad 04 00 52 06 ...`
    - `443` 字节服务端包：`28 b5 2f fd 60 6c 01 1d 08 00 d2 08 ...`
    - `395` 字节服务端包：`28 b5 2f fd 60 d8 00 9d 06 00 c4 09 ...`
  - 当前已经能再往前推进一层：
    - 这几包里的 `28 b5 2f fd` 不只是“看起来像 zstd 魔数”
    - 它们的 `frame_descriptor / frame_content_size / block_header / block_size` 这层都能解释成结构上合理的 zstd 帧头
    - 例如：
      - `443` 字节服务端包：`frame_content_size = 364`，`block_size = 259`
      - `395` 字节服务端包：`frame_content_size = 216`，`block_size = 211`
  - 把这些包从 `28 b5 2f fd` 起点直接送给标准 `zstd` 解压会失败
    - 当前会明确报 `Data corruption detected`
  - 所以这几包更像是“`penc` + 字典化/加密壳”，不是前面 `419/345/611` 那种标准 Zstd 帧
  - Windows dump 样本和真实线上包之间，当前已经能自动跑出更强的对应关系：
    - 真实客户端 `271` 字节壳包，已经能和至少 `2` 份 Windows dump 精确命中：
      - `spawn_dump_271_1774689981.bin`
      - `spawn_dump_271_1774690043.bin`
    - 真实客户端 `333` 字节壳包，也已经能和至少 `2` 份 Windows dump 精确命中：
      - `spawn_dump_333_1774689982.bin`
      - `spawn_dump_333_1774690043.bin`
    - 旧的近似样本也仍然有价值：
      - `dump_271_1774689481.bin` 和真实客户端 `271` 字节包只差 `1` 个字节
        - 差异偏移是 `239`
        - dump 值 `0x54`
        - 线上值 `0x01`
        - 这个偏移已经落在 `penc` 后的块内容里，不在 `网络包` 头或 zstd 帧头字段里
      - `dump_334_1774689490.bin` 和真实客户端 `333` 字节包虽然不相等
        - 前 `32` 字节完全一致
        - `field32/field36` 仍然只差 `334 vs 333`
        - 更主要的差异量继续集中在 `zstd-like` 块体内部，而不是外层 `网络包` 头
    - 上面这组结论现在已经落成独立接口：
      - `GET /api/debug/auth-7100-client-shell-correlation`
    - 这组壳包对位现在又扩成了双向总览接口：
      - `GET /api/debug/auth-7100-shell-correlation`
      - `POST /api/debug/auth-7100-shell-correlation`
        - 请求体：`{ "path": "/abs/path/to/file.pcap" }`
        - 也可以直接给 `.pcapng`；服务端会调用系统 `tcpdump` 临时转换成 classic `pcap`
        - 还支持可选过滤：
          - `local_endpoint`
          - `session_role`
          - `source_endpoint`
          - `destination_endpoint`
      - 当前 sample pcap 会统一列出：
        - 客户端 `611 / 271 / 333`
        - 服务端 `443 / 395`
      - `271 / 333` 继续保持 exact dump 对位
      - 当前 match 明细还会同时返回 `zstd_block_body_compare`
        - 可直接看到块体级的 `equal_prefix_len / equal_suffix_len / diff_bytes / first_diff_offset`
        - 当前 `271` 的同长度近邻已经能明确成“只差 `1` 字节，且首个差异位于块体偏移 `58`”
      - `443 / 395` 因为仓库里没有同长度 dump，当前只会走近邻长度 fallback
        - 当前首个近邻样本都落在 `dump_411_1774689402.bin`
      - `220 / 273` 仍然只在 timeline 文档里出现，当前 sample pcap 还没覆盖
  - 这说明：
    - 当前 Windows dump 集合里，已经明确混有“与真实出网壳包处于同一阶段”的样本，而不只是“相邻阶段”的近似样本
    - 后续逆向重点应继续放在 `penc` 后的块体变换，而不是重新怀疑 `网络包` 头或 zstd 帧头解析

- `183.236.97.134:14017` 这一组后续流量已单独验证：
  - 客户端重组流 `2673` 字节，服务端重组流 `10272` 字节
  - 从偏移 `0` 开始就是高熵随机二进制，没有 `网络包` 头，也没有 `OEM_DATA_HEAD`
  - 当前 `stream_analyze` 输出：
    - 客户端：`entropy=7.930 ascii-ish=37.5% zeroes=0.4%`
    - 服务端：`entropy=7.981 ascii-ish=38.5% zeroes=0.4%`
  - 这说明 `14017` 很可能已经进入另一个加密/混淆后的业务通道，而不是继续沿用前面的明文 `网络包` 外壳

- `7719` 行情流已经能直接从“重组后的 raw TCP 字节流”里恢复应用层帧，不再需要回到 `pcap` 手工对长度：
  - 客户端流 `1742` 字节一共 `5` 帧：
    - `@0    op=0x440c sub=0x2b02 flags=0x0100 payload=572`
      - inner `tag=0x054c kind=0x0005`
      - `80` 个代码：`000973 .. 001313`
    - `@582  op=0x6b0c sub=0x0a07 flags=0x0200 payload=250`
      - inner `tag=0x054c kind=0x0005`
      - `34` 个代码：`002263 / 600666 / ... / 000862`
    - `@842  op=0x450c sub=0x2a02 flags=0x0100 payload=48`
      - inner `tag=0x0547 kind=0x0004`
      - `4` 个指数代码：`399002 / 399003 / 999998 / 999997`
    - `@900  op=0x460c sub=0x2b02 flags=0x0100 payload=572`
      - inner `tag=0x054c kind=0x0005`
      - `80` 个代码：`001314 .. 002034`
    - `@1482 op=0x6c0c sub=0x0a07 flags=0x0200 payload=250`
      - 同第二帧的 `34` 个代码
  - 这组客户端请求的头格式已经明确：
    - 固定头长 `10` 字节
    - `bytes[6..8] == bytes[8..10] == payload_len`

- `7719` 服务端流 `20665` 字节也已经能稳定逐帧切开：
  - 固定头长 `16` 字节，前缀 `b1 cb 74 00`
  - 头字段规则：
    - `bytes[12..14] == compressed_len`
    - `bytes[14..16] == original_len`
  - 当前 `5` 帧分别是：
    - `@0     op=0x441c sub=0x2b02 tag=0x054c body=7093 original=8101`
    - `@7109  op=0x6b1c sub=0x0a07 tag=0x054c body=3196 original=3586`
    - `@10321 op=0x450c sub=0x2a02 tag=0x0547 body=2 original=2`
      - 正文固定为 `93 93`
    - `@10339 op=0x461c sub=0x2b02 tag=0x054c body=7098 original=8105`
    - `@17453 op=0x6c1c sub=0x0a07 tag=0x054c body=3196 original=3586`
  - 其中 `4` 个大回复正文都是标准 `zlib`：
    - 第一/三大帧解压后前缀是 `00 00 50 00 00`
      - `status = 0`
      - `count = 80`
    - 第二/四大帧解压后前缀是 `00 00 22 00 00`
      - `status = 0`
      - `count = 34`
  - 这说明 `7719` 至少有一条“代码列表请求 -> zlib 压缩逐股记录回复”的标准业务通道

- `7708` 服务端流也复用了同样的 `16` 字节外层头：
  - `2731` 字节流可切为 `5` 帧：
    - `@0    op=0x020e sub=0x01ab tag=0x0bb9 body=59  original=59`
    - `@75   op=0x032e sub=0x01ab tag=0x0fa0 body=892 original=850`
    - `@983  op=0x043e sub=0x01ab tag=0x0fa0 body=404 original=1310`
    - `@1403 op=0x052e sub=0x01ab tag=0x0fa0 body=892 original=850`
    - `@2311 op=0x063e sub=0x01ab tag=0x0fa0 body=404 original=1310`
  - 但这些正文当前还不是标准 `zlib`
  - 客户端 `7708` 流只有首个 `912` 字节请求能明显看出一段自描述头，后续 `283` 字节请求的帧头规则还没完全恢复

- `7709` 已确认不是随机业务流，而是同一套 `client10/server16` 帧协议里的“代码表批量下发”通道：
  - 小握手流 `2655 -> 120.195.71.160:7709`
    - 客户端只有 `12` 字节，一帧：
      - `op=0x010c sub=0x0000 payload=2 body=15 00`
    - 服务端 `143` 字节，一帧：
      - `op=0x011c sub=0x0000 tag=0x0015 body=127 original=427`
      - `zlib` 解压后能直接看到：
        - `20260328_085435`
        - `ININ Cloud V6.72`
        - `/tdx/hostl/`
  - 主数据流 `2400 -> 120.195.71.160:7709`
    - 客户端 `1147` 字节，共 `53` 帧
    - 第 `3` 帧正文里可以直接读到 `tdxlevel2`
    - 后续 `50` 个 `6` 字节请求体都满足：
      - `le_u16(body[0..2]) == 0x0450`
      - `le_u16(body[2..4]) == bucket`
      - `le_u16(body[4..6]) == offset`
    - 其中：
      - `sub=0x6d00` 对应 `bucket=0`
      - `sub=0x6e00` 对应 `bucket=1`
      - `offset` 以 `1000` 为步长递增
  - 服务端 `445457` 字节，共 `53` 帧
    - 第一条 `zlib` 小回复里还出现 `www.tdx.com.cn`
    - 后续 `50` 个大回复都满足：
      - `tag=0x0450`
      - `zlib` 解压后大小基本是 `29002` 字节
      - 前 `2` 字节 `le_u16 == 1000`
      - 后面正好是 `1000 * 29` 字节记录
    - `29` 字节记录当前已能稳定解析出：
      - `code[7] + nul`
      - `gbk-name[8]`
      - 其余 `13` 字节元数据
    - 示例：
      - 首块：`395001d 主板Ａ股` .. `002111d 威海广泰`
      - 小工具现在已经能直接打印每个块的首尾代码和名称
  - 这说明：
    - `7708/7709/7719` 至少共享同一族应用层帧头
    - `7709` 在当前样本里承担的是“代码表/基础资料批量同步”，不是 `14017` 那种随机加密流
  - Linux 侧已经对 `7709` 做过直连回放：
    - 小握手 `2655 -> 7709`
      - 原样发送 `12` 字节就能稳定收到 `143` 字节回复
      - 回复仍是同一个 `server16 + zlib` 结构
    - 主数据流 `2400 -> 7709`
      - 如果把 `1147` 字节一次性整包发完，服务端直接 `EOF`，回复 `0`
      - 但如果按 `53` 个 client10 帧边界分段发送，并在每段后留一个短接收窗口：
        - 可以稳定收到完整 `445457` 字节 reply
        - `quote_frame_scan` 解析结果与原始抓包同构
      - 再按 `server16` 帧逐帧比对：
        - 共 `53` 帧
        - 只有第 `1` 个小元数据帧不同
        - 其余帧头一致
        - 所有后续 `0x0450` 代码表块正文都与抓包原样一致
  - 这说明纯 Rust/Linux 这条路已经不只是“能连通节点”，而是已经能把 `7709` 这条代码表同步子协议稳定重放出来
  - 进一步已经做成了原生同步例子 `examples/tdx7709_sync.rs`
    - 可复用实现已下沉到 `src/tdx7709.rs`
    - 它不再依赖抓包 replay，而是直接构造：
      - `3` 个 bootstrap `client10` 请求
      - `50` 个 `0x0450 + bucket + offset` 代码表块请求
    - 线上直连 `120.195.71.160:7709` 的一次实测结果：
      - reply 总长 `445457` 字节
      - `53` 个 `server16` 回复帧
      - 前 `3` 个 bootstrap 回复和抓包一致
      - 后续 `50` 个块回复全部可正常 `zlib` 解压
      - 原始 `50 * 1000 = 49760` 个记录槽位经 `code` 去重后得到 `49040` 条记录
      - 成功导出 `/tmp/tdx7709_codes.csv`
    - 代码表名称字段已确认是固定 `8` 字节 `GBK` 槽：
      - 少量记录会在最后一个字节留下半个双字节字符
      - 例如 `159007d` 的名称区原始字节是 `d1 f8 d6 b3 45 54 46 bb`
      - 需要从尾部回退 1 字节后再按 `GBK` 解码，才能稳定得到 `养殖ETF`
      - 同类脏记录当前样本里有 `18` 条，修正后都不再阻断导出
    - 导出样本头尾：
      - `000001d,上证指数`
      - `999999d,上证指数`
    - 还可见特殊样本：
      - `880654d,ChatGPT`
  - 进一步对完整 `pcap` 做首帧归并后，当前 `7709` 客户端流只剩两类模板：
    - `20` 条 `347/1147` 字节主会话
      - 其中首个 `292` 字节 bootstrap 完全一致，没有按连接变化的参数位
    - `5` 条 `12` 字节探活/测速流
      - 正文固定为 `0c0100000000020002001500`
      - 分别打到 `124.71.163.106 / 124.70.183.173 / 180.101.48.175 / 120.195.71.160 / 122.96.107.241`
    - 这说明当前样本里“主会话 bootstrap”和“多节点探活”是两条独立模板，不是同一个 `292` 字节包里埋了动态节点参数
  - 这轮把 `quote-frame-scan` 再结构化了一层，客户端 `1147` 字节主会话现在已经能直接打出语义标签：
    - `frame[0] op=0x010c sub=0x7b00 payload=282`
      - `bootstrap.main-site-validate`
    - `frame[1] op=0x020c sub=0x9400 payload=3`
      - `bootstrap.server-info`
    - `frame[2] op=0x030c sub=0x9900 payload=32`
      - `bootstrap.ticket-register`
      - body 里能直接看见 `tdxlevel2`
    - `frame[3..25] sub=0x6d00 payload=6`
      - `bootstrap.code-table-chunk.bucket0`
    - `frame[26..52] sub=0x6e00 payload=6`
      - `bootstrap.code-table-chunk.bucket1`
  - 同时 `quote-frame-scan` 还补了 `8` 字节块统计，方便继续盯 `0x7b00` 这条首帧：
    - `282` 字节 body 会按 `35 * 8 + 2` 切开
    - 当前样本里完整 `8` 字节块总数 `35`
    - 唯一块只有 `13`
    - 最常见块是 `03 57 74 99 33 ae 27 70`
      - 重复 `21` 次
    - 尾部余数 `2` 字节当前会单独保留
    - 这进一步支持了“首帧 body 不是自然结构体直发，而是某种按固定块处理后的结果”这一判断
  - 这轮又把登录后阶段标签补进去了，`quote-frame-scan` 的摘要现在会直接给出 `phase_order / phase_counts`：
    - `0x7600 / 0x7601` -> `post-login.bulk-record-29b-*`
    - `0x7501 / 0x7502` -> `post-login.fin-143b-*`
    - `0x2900 / 0x2a00 / 0x2902 / 0x2a02 / 0x2802` -> `post-login.quote-0547-*`
    - `7719` 里的 `0x2b02 / 0x0a07 / tag=0x054c` 也会单独标成 `post-login.quote-054c-*`
  - 这轮又把统计对齐修正了一层：
    - 之前的 `block8` 是从整个 body 起点算，包含了前导 `tag = 0x000b`
    - 如果先去掉这 `2` 字节 tag，再按 `8` 字节切，剩余密文区正好是 `280 = 35 * 8`
    - 对齐后唯一块只有 `9`
    - 最常见块变成 `74 99 33 ae 27 70 03 57`
      - 重复 `25` 次
      - 位置分布当前可直接列成：
        - `2, 3, 4, 5`
        - `8, 9, 10, 11`
        - `16, 17, 18, 19, 20, 21`
        - `24, 25, 26, 27, 28, 29`
        - `32, 33, 34`
      - 另外两个重复 `2` 次的 `8` 字节块分别落在：
        - `a8 74 d3 16 27 61 9e e5` -> 位置 `0, 22`
        - `df b7 91 87 64 9c 82 89` -> 位置 `1, 23`
    - 这说明“真正按固定块处理”的对象不是 `282` 字节整 body，而是 `tag` 后面的 `280` 字节主体；这个结论比之前的未对齐统计更可信
    - 当前更稳妥的表述是：
      - `payload[0..2] = 0x000b` 只是前导 tag
      - `payload[2..282] = 0x118` 主体，极可能对应 `0x10066b60` 组好的明文 body 经 `Tdx_Encrypt` 一类固定块处理后的等长结果
      - 现阶段还不应该把这 `0x118` 主体内部再硬命名成更细的明文字段偏移
    - 下一步最小可行实验已经收敛：
      - 在 Windows 上只盯 `0x10066b60 -> 0x1007b7c0/0x1002bf90`
      - 一次 dump `0x10066b60` 产出的 `0x118` 明文
      - 一次 dump 出网前 `0x7b00` 的 `payload[2..]`
      - 把这两份 `0x118` 做一一比对，而不是继续猜测协议字段

- Linux 侧已经做过“脱离 DLL 直接回放公网行情节点”的验证：
  - `110.41.14.158:7719`
    - 一次性回放整条 `1742` 字节客户端流：连接成功，但回复长度为 `0`
    - 按抓包节奏分段回放：
      - `0:582:0`
      - `582:260:420`
      - `842:58:550`
      - `900:582:2010`
      - `1482:260:444`
    - 结果是在第二帧 `260` 字节发送后收到 `RST`，没有任何 reply body
    - 仅回放 `7709` 的 `12` 字节探活帧时，会稳定收到 `139` 字节回复：
      - 同样是 `server16 + zlib`
      - `op=0x011c sub=0x0000 tag=0x0015 body=123 original=427`
      - 解压后可见：
        - `20260328_085110`
        - `Level2  V6.71`
        - `/tdx/hostl/`
        - `FA163EE04559`
    - 但“先探活，再发 `292` 字节主站校验帧”时，第二步仍然返回同一个 `96` 字节错误包：
      - `op=0x010c sub=0x7b00 tag=0x000b body=80 original=80`
      - 正文仍是 `行情主站校验,无数据参数`
    - 再继续发送借来的 `13 + 42` 字节 `7709` bootstrap 尾帧后，会立即 `RST`
    - 这说明 `7719` 和 `7709` 至少共享探活/选站层，但探活阶段不会给 `292` 字节主站校验补足参数
  - `221.236.15.16:7708`
    - 按抓包节奏分段回放：
      - `0:912:1168`
      - `912:128:1552`
      - `1040:155:127`
      - `1195:283:643`
      - `1478:283:2903`
      - `1761:283:139`
    - 第一帧 `912` 字节后会收到一个 `20` 字节短回复：
      - `b1 cb 74 00 0e 02 ab 01 00 00 b9 0b 04 00 04 00 00 00 00 00`
      - 用 `quote_frame_scan` 解析就是：
        - `op=0x020e`
        - `sub=0x01ab`
        - `tag=0x0bb9`
        - `body=4`
        - `original=4`
        - 正文 `00 00 00 00`
    - 在继续发送 `128 + 155` 字节后被 `RST`
    - 单独回放同一个 `12` 字节探活帧时，没有任何 reply body
    - 先探活再发 `912` 字节首帧，会比原始重放更早收到 `RST`
  - 这个实验至少证明了两点：
    - Linux 主机可以直接和 `7708/7719` 行情节点建立 TCP
    - 但单独重放业务包还不足以复现成功会话，行情节点依赖更早的认证/主站/握手上下文

- 进一步把 `2923 -> 14017` 当作前置流串联回放，也已经单独验证过：
  - 先按抓包节奏完整回放 `192.168.3.38:2923 -> 183.236.97.134:14017`
    - `81 -> 1400 -> 419 -> 665 -> 108`
    - 服务端会正常返回 `9683` 字节 reply
  - 然后立即再按抓包节奏回放 `9278 -> 7719`
  - 结果仍然是在 `7719` 第二帧 `260` 字节发出后收到 `RST`
  - 这说明“只补上当前抓包里最早那条完整 `14017` 建连”仍不足以复现 `7719` 会话

- `pcap_flow_timeline` 已经把当前 `capture_netzip_full.pcapng` 里的关键时序钉住：
  - `9278 -> 110.41.14.158:7719`
    - 首个可见包就是 `flags=AP payload=582`
    - 没有 `SYN / SYN-ACK`
    - 这是“中途接入的现成业务会话”，不是抓包里的会话起点
  - `1871 -> 221.236.15.16:7708`
    - 首个可见包是纯 `ACK`
    - 后续才出现 `912` 字节业务 payload
    - 同样属于 midstream
  - `2925 -> 123.125.108.156:7708`
    - 只有两次 `SYN` 重试
    - 没有任何 payload，也没有 `SYN-ACK`
    - 这不是业务 bootstrap，只是失败的连接尝试
  - 在首个 `7719` payload 出现前，抓包里唯一完整建连成功的 `14017` 流就是 `2923 -> 183.236.97.134:14017`
  - 因此当前最像缺口的，不是“还有另一条已抓到但没重放的 `14017` 前置流”，而是 `7719/7708` 自身更早的会话建立字节并不在这份抓包里

- 2026-03-28 又补了一轮 `capture_timeline_run.pcapng`：
  - 这轮样本不是“完美冷启动、单实例、全程按 PID 绑定”的定案样本
    - 当时系统里曾被诊断动作额外拉起过一个 `网际风.exe`
    - 因此这里把它当作“强协议证据”，不把它单独当作唯一归因证据
  - 截至 `2026-03-29` 当前仓库快照里，已经找不到 `captured_windows_traffic/capture_timeline_run.etl` / `capture_timeline_run.pcapng`
    - 但本机 `~/.codex/history.jsonl` 仍保留了当时的保全记录，路径就是这两个文件名
    - 因而这里关于 `7128 / 14124 / 14715 / 14717` 的结论可以确认来自真实离线分析，不是事后凭文档倒推；只是当前快照没把原始文件带进来
  - 但把 `pcapng` 转成经典 `pcap` 后，`pcap_flow_timeline` 已经能确认几个新事实：
    - 过滤 `host=192.168.3.38`、`ports=6100,7100,7708,7709,7719,14017` 后，共命中 `88` 条流
    - 这轮样本里真正出现并从 `SYN` 开始抓到的，只有：
      - `39.108.103.69:7100`
      - `120.195.71.160:7709`
    - 当前没有看到真实 `TCP 5-tuple` 落在：
      - `6100`
      - `7708`
      - `7719`
      - `14017`
  - `7100` 侧现在不只是“有一条登录包”：
    - 这轮至少能确认 `3` 条从 `SYN` 开始的短 `测速` 会话：
      - `39.108.103.69:7100 <-> 192.168.3.38:7128`
        - 客户端 `413` 字节：`认证 | 请求=测速`
        - 服务端 `341` 字节：`认证 | 请求=测速` 回复
      - `39.108.103.69:7100 <-> 192.168.3.38:14124`
        - 客户端 `413` 字节：`认证 | 请求=测速`
        - 服务端 `345` 字节：`认证 | 请求=测速` 回复
      - `39.108.103.69:7100 <-> 192.168.3.38:14715`
        - 客户端 `415` 字节：`认证 | 请求=测速`
        - 服务端 `341` 字节：`认证 | 请求=测速` 回复
    - `39.108.103.69:7100 <-> 192.168.3.38:14717`
      - `started-with-handshake`
      - 客户端重组后 `1434` 字节，共 `4` 个 `网络包`
        - `610` 字节：`认证 | 请求=登录`
          - 直接字段线索：
            - `账号=168`
            - `密码=168`
            - `运营商名称=泉州移动`
            - `模块=股票客户端`
        - `271` 字节：`压缩|ZSTD字典`
        - `220` 字节：`压缩|ZSTD字典`
        - `333` 字节：`压缩|ZSTD字典`
        - `610` 字节登录包里已能直接读到：
          - `网际风.exe`
          - `Stock.dll`
          - `Stock.字典`
      - 服务端重组后 `2649` 字节，共 `4` 个 `网络包`
        - `441` 字节：`压缩|ZSTD字典`
        - `1540` 字节：`数据 | 下载文件`
          - 直接字段线索：
            - `请求=系统\\通达信股票服务器.ini`
            - `来源=认证服务器`
            - `名称=系统\\通达信股票服务器.ini`
            - `压缩=无压缩`
        - `273` 字节：`压缩|ZSTD字典`
        - `395` 字节：`压缩|ZSTD字典`
        - `1540` 字节下载包里已能直接读到：
          - `系统\\通达信股票服务器.ini`
        - `395` 字节壳包的原始文本线索里已能看到：
          - `Tdx_Encrypt`
    - 这把 `7100` 的角色拆清了：
      - 短会话负责 `认证 | 请求=测速`
      - 长会话负责 `认证 | 请求=登录 -> 数据 | 下载文件 -> penc / ZSTD字典` 后续壳
  - `7709` 侧这轮则拿到了完整的 `SYN -> bootstrap -> 代码表 -> post-login 行情` 长会话：
    - 整个 timeline 样本里一共能数到 `84` 条 `7709` 流，分成 `4` 轮，每轮都是：
      - `1` 条 `12 -> 143` 的短 probe
      - `20` 条从 `292B 0x7b00` 开始的主流
    - 第一轮的本地端口分布是：
      - probe：`7163`
      - 主流：`7171, 7172, 7181, 7183, 7190, 7193, 7195, 7198, 7200, 7201, 7203, 7206, 7208, 7209, 7212, 7214, 7216, 7217, 7218, 7219`
    - 后续三轮同样保持 `1 + 20` 的结构：
      - 第二轮：`14193` probe + `14203..14245` 主流
      - 第三轮：`14570` probe + `14574..14636` 主流
      - 第四轮：`14750` probe + `14775..14823` 主流
    - 因而当前可以把 `12B probe` 和真正主流明确分开：
      - probe 的共同特征是：短连接、首个客户端 payload 就是 `12B`、服务端只回 `143B`
      - 主流则是：`292B 0x7b00 -> 13B 0x9400 -> 42B 0x9900 -> 代码表 -> 0x0547`
    - `120.195.71.160:7709 <-> 192.168.3.38:7171`
      - `started-with-handshake`
      - 时长约 `633.8s`
      - 客户端重组后 `181010` 字节
      - 服务端重组后 `2793306` 字节
    - 这条长流里，客户端不再只是旧样本里的 `347/1147`：
      - 开头仍是标准 bootstrap：
        - `0x010c / 0x7b00 / tag=0x000b / payload=282`
        - `0x020c / 0x9400 / payload=3`
        - `0x030c / 0x9900 / payload=32 / ascii=tdxlevel2`
        - 对照本地 `rustHq` 的公开通达信实现后，这里又多了一层佐证：
          - [packet.rs](/home/codes/_third_party/rustHq/src/packet.rs#L114) 和 [packet.rs](/home/codes/_third_party/rustHq/src/packet.rs#L118) 里的标准握手 `packet_2 / packet_3`，长度也正好是 `13B / 42B`
          - 也就是说，当前样本更像“厂商自定义的 `0x7b00` 首帧 + 后续回到 TDX 家族 bootstrap”，不是完全另一套独立协议
      - 随后是 `0x6d00 / 0x6e00` 的 `0x0450 + bucket + offset` 代码表请求
      - 代表流里的 post-login 阶段现在已经能分成 3 段：
        - 第一段是 `0x7600 / 0x7601`
          - 客户端从 `frame[53]` 一直持续到 `frame[280]`
          - 大多数请求都是固定 `payload=354`
          - 这一段和 DLL 侧 `0x76000f`、`每股 0x1d = 29B` 回复的结论一致
        - 第二段是 `0x7501 / 0x7502`
          - 客户端从 `frame[281]` 持续到 `frame[595]`
          - 大多数请求都是固定 `payload=144`
          - inner `tag=0x0010 kind=0x0014`
          - 每帧都是 `20` 只股票代码
          - 这和 DLL 侧 `0x750010`、`每股 0x8f = 143B` 回复的结论一致
          - 再对照本地 `rustHq` 的公开 TDX 实现后，这一段几乎已经能直接定名成“批量财务”：
            - [packet.rs](/home/codes/_third_party/rustHq/src/packet.rs#L168) 的标准财务请求低 `16` 位就是 `0x0010`
            - [parser.rs](/home/codes/_third_party/rustHq/src/parser.rs#L352) 的标准财务正文正是 `2-byte prefix + N * 143B record`
            - 而 timeline 里实测的 `2862 = 2 + 20 * 143`、`1289 = 2 + 9 * 143` 与它完全一致
          - 因而这里不再只是“某段 post-login 固定长数据流”，而是高置信度的 `finance-info / 财务` 阶段
        - 第三段才是 `0x0547`
          - 中段先出现大正文的 `0x2900 / 0x2a00`
          - 尾段再出现周期性的 `0x2902 / 0x2a02`
          - 客户端请求体现在也已经能被 wire 直接拆开：
            - 固定 `payload=1104 = 4 + 100 * 11`
            - 也就是 `4` 字节小头，再跟 `100` 个 `11` 字节项
            - 单项结构已经能直接写成：
              - `1 byte market_flag`
              - `6 byte ASCII code`
              - `u32 little-endian token`
          - 这和 DLL 侧 `0x10062c40 / 0x10062f00` 的 `7-byte tuple + u32 token` 已经不是推断，而是线上请求体直接对上
          - 再结合 `rustHq` 的标准通达信实时包实现，这一段也有了更清楚的参照物：
            - [packet.rs](/home/codes/_third_party/rustHq/src/packet.rs#L71) 的公开 quote 请求是 `2 + N * 7`，每项只有 `market + code`
            - [parser.rs](/home/codes/_third_party/rustHq/src/parser.rs#L44) 的公开 quote reply 则是变长记录流，内部靠 [parser.rs](/home/codes/_third_party/rustHq/src/parser.rs#L620) 这种 `varint price` 继续解
            - 当前 `0x0547` 最像它的“带 token 的增量/续订变体”：请求项从 `7B` 扩成 `11B`，多出来的就是 `u32 token`
          - 这也解释了为什么当前线上 `0x0547` reply：
            - 平均只有 `109..123B / 股`，不是稳定定长
            - 解压后又搜不到明文 `6` 位代码
            - 更像“按 token / 内部对象流回传的变长结果”，而不是公开 TDX quote 那种直接带代码字符串的正文
      - 就当前这条长流而言，`0x0547` 的首次出现已经能精确到帧级：
        - 客户端 `frame[53..62]` 先连发 `10` 个 `0x7600` 批量请求
        - 客户端 `frame[63]` 首次出现 `op=0x400c sub=0x2900 tag=0x0547 payload=1104`
        - 客户端 `frame[65]` 随后出现 `op=0x420c sub=0x2a00 tag=0x0547 payload=1104`
        - 之后 `0x7600` 和 `0x2900/0x2a00` 交错推进
        - 提取 `0x0547` 请求体后，还能直接看到 token 的状态变化：
          - `frame[63]`：`item-count=100`，`token-nonzero=0`
          - `frame[65]`：`item-count=100`，`token-nonzero=0`
          - `frame[67]`：`item-count=100`，`token-nonzero=1`
          - 到尾段 `frame[604] / frame[605]` 这类 `0x2902 / 0x2a02` 时，已经变成 `token-nonzero=100`
        - 这正好和 DLL 侧“`0x100632b0` 先把 token 写回对象，再由 `0x10062f00` 继续发 `0x2a0547`”的闭环一致
    - 服务端同一长流也给出了对应 reply：
      - `0x7b00 / 0x9400 / 0x9900`
      - 大量 `0x6d00 / 0x6e00` 的 `29` 字节代码表 zlib 块
      - 代码表之后，中段先出现一批交错的 `0x7600-family` 和 `0x0547` 大正文回复：
        - `0x7600 / 0x7601` 这批大 zlib 回复从 `frame[53]` 持续到 `frame[280]`
        - 它们和 DLL 侧 `0x76000f`、`每股 0x1d = 29B` 的阶段顺序一致
        - 同时又已经能直接看到首批大的 `0x0547` 正文 reply：
          - `frame[63]`：`sub=0x2900 tag=0x0547 body=7412 original=11541`
          - `frame[65]`：`sub=0x2a00 tag=0x0547 body=6745 original=11493`
          - `frame[67]`：`sub=0x2a00 tag=0x0547 body=7117 original=12273`
          - `frame[69]`：`sub=0x2a00 tag=0x0547 body=6366 original=11032`
          - `frame[71]`：`sub=0x2a00 tag=0x0547 body=6112 original=10939`
        - 把这 `5` 个大正文用 `quote_tag_extract` 单独解出来后，还能再确认两件事：
          - 对应客户端 `frame[63/65/67/69/71]` 都各自正好带 `100` 只股票代码
          - 对应服务端解压正文长度分别是：
            - `11541`
            - `11493`
            - `12273`
            - `11032`
            - `10939`
          - 换算下来平均大约是 `109.39 .. 122.73` 字节/股
          - 这说明 `0x0547` 正文不像 `0x750010` 那样是稳定固定长记录流，而更像“带可变字段/压缩编码”的逐股结果
        - 原始解压正文里，确实都搜不到直接的明文 `6` 位股票代码 ASCII
          - 但拿本地公开 `7709` 实现 [rustHq packet.rs](/home/codes/_third_party/rustHq/src/packet.rs) / [rustHq parser.rs](/home/codes/_third_party/rustHq/src/parser.rs) 当参照，再试固定 `xor 0x93` 后，结构立刻开始露出来：
            - `frame063 -> count=100, filtered-records=100, first=[SH600000,SH600004,SH600006,SH600007,SH600008]`
            - `frame065 -> count=100, filtered-records=100, first=[SZ000815,SZ000816,SZ000818,SZ000819,SZ000820]`
            - `frame067 -> count=99, filtered-records=99, first=[SH113638,SH113640,SH113643,SH113644,SH113646]`
            - `frame069 -> count=100, filtered-records=100, first=[SH688690,SH688691,SH688692,SH688693,SH688695]`
            - `frame071 -> count=100, filtered-records=100, first=[SZ300945,SZ300946,SZ300947,SZ300948,SZ300949]`
          - 并且这些记录头不是伪命中：
            - 过滤条件是“前一字节必须是 `market in {0,1,2}`，后面 `6` 字节全是 ASCII 数字”
            - `filtered-records` 会和 `xor93 count` 精确对上
          - record gap 也能直接做出分布：
            - `frame063/065` 主要落在 `113..118`
            - `frame069/071` 主要落在 `105..112`
            - `frame067` 还出现一批 `143..148`
          - 这说明网络侧 `0x0547` 大 reply 不是“完全黑盒”，而是“`zlib` 之后再过一层固定 `xor 0x93`，里面仍然是逐股变长 record 流”
        - 进一步把 `xor93` 后的正文强行套进公开 `rustHq::parse_quote_body` 规则时：
          - 只要在前面补 `2` 字节空前导，就能直接读出第一条记录 `market=1, code=600000`
          - 但第二条开始会失去同步
          - 这说明它和公开 `0x053e` quote body 已经是近亲，不是完全无关的另一套编码；只是字段顺序/布局并不完全一样
        - 也就是说，这里不能再简单写成“先全是 `0x7600`，后面才开始 `0x0547`”
      - 再往后才是 `0x7501 / 0x7502` 的 `143B` 记录流：
        - 服务端从 `frame[281]` 持续到 `frame[595]`
        - 其中尾段 `frame[511..595]` 已能直接按 `20` 只股票一组看清
        - 代表帧例如：
          - `frame[511]`：`sub=0x7502 body=412 original=2862`
          - `frame[512]`：`sub=0x7502 body=433 original=2862`
          - `frame[595]`：`sub=0x7502 body=75 original=1289`
        - 这正好分别等于：
          - `2862 = 2 + 20 * 143`
          - `1289 = 2 + 9 * 143`
        - 因而这条 wire 证据已经不只是在长度上和 DLL 侧 `0x750010 -> 每次最多 20 只 -> 每条 0x8f = 143B` 对上
        - 它还和本地 `rustHq` 的公开通达信财务 parser 完整对上：
          - `2` 字节前缀
          - `143B` 单股记录
          - 低 `16` 位命令号 `0x0010`
        - 这使得 `0x750010 = 批量财务` 这一点已经从“高置信推断”提升到“强交叉印证”
      - 直到服务端尾段，才出现 `0x2902 / 0x2a02` 小回复，以及 `0x2802 / tag=0x0004` 的状态包：
        - `frame[596..603]` 是一串 `0x2802 / tag=0x0004` 状态包
        - `frame[604]` 首次出现 `op=0x5d0c sub=0x2902 tag=0x0547 body=2`
        - `frame[605..607]` 紧跟 `0x2a02 / tag=0x0547 body=2`
        - 当前这组 `body` 固定是 `93 93`
      - 并且 `0x0547` 不是只出现在这一条代表流里：
        - 第一轮代表流 `7171`，客户端侧能数到 `45` 个 `tag=0x0547`
        - 同一条流服务端侧也能数到 `45` 个 `tag=0x0547`
        - 后两轮代表流 `14574`、`14775` 各自也都还能看到 `5` 个 `tag=0x0547`
        - 之后继续出现同型 `0x2802 -> 0x2902/0x2a02` 小回复组
  - 因此，这轮 timeline 样本带来的最关键升级是：
    - `0x0547` 不再只是“从 DLL 和旧抓包交叉推断出来的 post-login 行情链”
    - 它已经在 `7709` 的完整长会话里被直接观测到
    - 同时，`0x750010 -> 每次最多 20 只 -> 每条 0x8f = 143B` 这条 DLL 阶段，也已经被 wire 长度直接印证
    - 而且现在连 `0x0547` 大正文也不再是“只知道 zlib 长度”的状态了：
      - 固定 `xor 0x93` 后，已经能直接露出 `count + market + 6位代码` 的逐股 record 头
      - `quote_0547_scan` 现在会自动打印这层 `xor93` 视角下的首批代码与 gap 分布
    - 当前剩下的问题，不是“它是不是 `7709` 上的真实业务”，而是：
      - `0x0547` 各字段如何逐项映射到 DLL 导出结构
      - 以及这份样本里 `0x0547` 服务端为什么只露出短 ACK，而更丰富的字段映射仍主要来自 DLL 侧逆向
  - 针对提取出来的前 `5` 份大 `0x0547` zlib 正文，又补了一个结构扫描：
    - 当前都看不到直接的 `6` 位代码串
    - 多份正文之间 `common-prefix = 0`
    - 只有很短的 `common-suffix = 2`
    - `0x9393` 是明显的高频 token：
      - `frame063 -> 790`
      - `frame065 -> 1341`
      - `frame067 -> 1025`
      - `frame069 -> 1097`
      - `frame071 -> 1350`
    - 最大同字节连续 run 也不小：
      - `5 / 33 / 7 / 35 / 33`
    - 同一组 `frame063` 的客户端请求 payload 和服务端解压正文之间，也找不到公共 `4` 字节窗口
    - 这更像“zlib 之后还有一层内部编码/对象流”，而不是直接把请求代码和返回字段明文排在一起

- 后续又重组了多条从 `SYN` 开始的 `14017` 完整流：
  - `7466 <-> 14017`
    - 客户端 `3334` 字节，服务端 `2209` 字节
  - `2394 <-> 14017`
    - 客户端 `6367` 字节，服务端 `11458` 字节
  - `8826 <-> 14017`
    - 客户端 `27527` 字节，服务端 `7010` 字节
  - `7834 <-> 14017`
    - 客户端 `2593` 字节，服务端 `3088` 字节
  - 这些流和最早的 `2923 <-> 14017` 一样：
    - 从偏移 `0` 开始就是高熵二进制
    - `stream_analyze` 仍然看不到 `网络包`、`OEM_DATA_HEAD` 或稳定的明文字段
    - 彼此之间找不到公共的 `4/6/8` 字节窗口
    - 和当前 `7719/7708` 客户端 raw 流之间也找不到公共的 `4/6/8` 字节窗口
  - 这更像“每连接随机化/加密”的二进制通道，而不是一个带固定包头的静态长度协议

- `Stock.字典` 文件已经核对过：
  - 它是 `1000` 字节的 `UTF-16LE` 文本词典
  - 内容包含 `认证 / 网络包 / penc / 加密包 / 加密解密 / 大智慧S_网络配置 / 大智慧S_指标授权 ...`
  - 它不是标准 `.zdict` 二进制字典文件
  - 直接拿它喂 `zstd -D` 无法解开这些 `field44 = 12` 的包

- 已从 `Stock64.dll` 内部进一步确认：外层不是简单长度头，而是“自描述对象 + 中文字段名”的通用对象模型。
  当前已能稳定定位这些对象/字段名：
  - 对象名：`网络包`
  - 核心字段：`数据`
  - 压缩相关：`压缩`、`长度`、`原长度`
  - 其他字段：`扩展`、`错误`、`哈希值`、`属性`

- 已定位到独立的 `网络包` / `数据` 常量，而不只是 `解压网络包出错` 里的子串：
  - `网络包` 常量位于 `0x180070658`
  - `数据` 常量位于 `0x180070670`

- `dll_data_xrefs` 现在会直接打印目标地址附近的 ASCII / UTF-16 字符串，因此中文常量已经能直接读出上下文。
  目前最关键的一组是：
  - `0x18006f388 -> 提示信息`
  - `0x18006f3dc -> 节点`
  - 同一簇附近还能读到：`等号`、`注释`、`说明`、`GET /`、`HTTP`
  - 结合 `0x18000ae30 -> 0x18000b120` 的调用关系，这一簇更像 HTTP/配置节点解析器，不像 `penc / Tdx_Encrypt` 的真实解密核心

- 已确认的 DLL 内部调用链：
  - `0x1800129a0`
    - 这是前面误记为 `0x180012a10` 的真实函数起点
    - 构造 `网络包` 对象
    - 把内部对象挂到 `数据` 字段
  - `0x180012ad0`
    - 从 `网络包` 对象取出 `数据`
  - `0x1800128c0`
    - 通用发包封装入口
    - 内部会调用 `0x1800129a0` 生成 `网络包`
    - 然后通过上下文对象虚表 `+0x30` 把 `网络包` 发出去
  - `0x1800127e0`
    - 通用文本请求派发入口
    - 已确认有硬编码请求：`系统?请求=心跳包`
  - `0x1800093f0`
    - 把 `数据` / `扩展` 两个字段复制到目标对象
  - `0x1800093f0 -> 0x1800094e4...0x180009750`
    - 处理解压逻辑
    - 检查 `压缩`
    - 读取 `原长度`
    - 解压后回写 `数据`
    - 更新 `长度`
    - 失败时写入 `错误=解压网络包出错`
  - `0x18000c460...0x18000c6a2`
    - 发送侧压缩/不压缩分支
    - 先写 `原长度`
    - 计算并写 `哈希值`
    - 写 `属性`
    - 如果压缩后更小：
      - `数据 = 压缩后缓冲区`
      - `长度 = 压缩后长度`
      - `压缩 = 1`
    - 否则：
      - `数据 = 原始缓冲区`
      - `长度 = 原始长度`
    - 目前只看到“未压缩 / 标准压缩成功”这类常规分支，没有在这条路径上直接看到与抓包 `field44 = 12` 一一对应的处理
  - `0x180010440`
    - 直接向 socket 发送缓冲区
    - 会处理部分发送和 `10035 / WSAEWOULDBLOCK`
  - `0x180012520...0x1800125dd`
    - 已确认是“上传文件”发送路径
    - 先调用 `0x1800129a0` 包成 `网络包`
    - 再调用 `0x180010440` 落到 socket 发送

### `0x0547` 公共行情时间归一化（2026-07-28）

- 同一轮跨市场实测中，Rust 原始 `0x0547` 时间稳定表现为：沪市主板、科创板和
  ETF 为 `153050`，深市主板、创业板和 ETF 为 `153000`，北交所为 `150000`。
  价格、昨收、开高低、累计量和当前量均保持对齐，排除了时间字段导致的记录游标错位。
- Rust 直连 Wine 当前使用的同一上游 `180.101.48.175:7709` 得到相同原始时间，
  排除了上游服务器差异。Wine `Ask(股票数据?请求=实时数据...)` 的公共对象则把沪深
  都返回为 `15:00:00`。
- 对 `127.0.0.1:16801` 抓包后确认，本地返回包已经包含完整 `OEM_REPORT`；转换边界
  在 `网际风.exe` 本地桥内部，不在远端 `7709`，也不能归因于 quoteGateway。
- 当前生产 `/home/third_party/quoteNetzipWine/网际风.exe` SHA-256 为
  `de712a8dde6d990e1c586f8afd4194575e35dffa2d0f81245fe29f6f8509bd29`。
  其 `0x4839a3` 先比较 `source + 0x21` 与 `0x249f0`（十进制 `150000`），超过时
  使用 `150000`；`0x438800` 再执行 `HH * 3600 + MM * 60 + SS`，结果加日期基值后
  写入输出对象 `+0x0c`。
- 因而公共语义已闭环为 `public_time = min(valid_source_time, 150000)`。NetzipRs
  保留 `time_hhmmss_raw` 和 `extra0_time_hhmmss` 供诊断，仅对外
  `datetime / quote_datetime` 应用公共时间规则；非法时间仍拒绝，不会先截断成有效值。
- 对 quoteGateway 的行协议从 `netzip-rust-7709-0547.v1` 升为 `v2`。网关仅在缓存行
  为 v1、入站行为 v2 时允许公共时间向 `15:00:00` 迁移；普通同版本倒序仍拒绝，
  同时避免通过重启或清缓存掩盖语义变更。
- 本文后面的 `Stock.dll 0x1007e4d0 / 0x1007ef80` 属于历史 DLL 样本的结构分析。
  当前生产 `Stock.dll` SHA-256 为
  `524f3d11ab53211d437ded738bb1a00d29b11a16aed8167518c9aa982a9084df`，不能在未重新
  定位的情况下沿用旧地址或据此归因当前公共时间转换。

### `0x0547` 累计成交量归一化（2026-07-28）

- 对同一次生产全市场请求中的 `5532` 个代码做 Wine/Rust 对照，得到 `5194` 个可比样本。
- 修复前所有可比样本都满足 `Wine.volume == f32(wire_volume + 1)`，命中率为
  `5194 / 5194`。其中 `2630` 个样本的源行情时间完全相同：
  - `2629` 个样本直接表现为 Wine 公共累计量比 Rust wire 值大 `1`。
  - `000725` 的 wire 值为 `17194932`，超过 `f32` 的逐整数精度范围，所以 `+1`
    后转成 `f32` 表面仍相等；它没有否定同一个公式。
- 因而差异来源已定位在 `0x0547` 字段语义，而不是游标错位、采样时间不同或
  quoteGateway 补偿。当前解码规则是：正的累计量返回 `wire + 1`，wire `0` 保持 `0`。
- `current_volume` 不使用这条规则。真实 `SH600000` 样本在修复前的 wire 字段为：
  - 累计量 `392439`
  - 当前量 `7687`
  - 成交额 `393449088`
  - 对应 OEM 公共累计量为 `392440`
- 成交额仍未闭环，不允许按表面差值加固定补偿。`5194` 个可比样本中，
  `Wine.amount - Rust.amount` 为正、负、相等的数量分别为 `3492 / 1670 / 32`，
  差值和 ULP 差都没有固定模式。历史 OEM 对象中 `SH600000` 的公共 `amount` 为
  `393447264`，内部 `nowa` 为 `393449088`；后者与现有 wire 样本相同，但当前没有证据
  证明它们来自同一帧。
- 现存 `120.195.71.160:7709 <-> 192.168.3.38:2400` pcap 可重组出服务端
  `445457` 字节、`53` 帧，但数据恰好结束在 `0x0547` 之前，不能用于同帧成交额对照。
- 当前生产 DLL 是 `/home/third_party/quoteNetzipWine/Stock.dll`，SHA-256 为
  `524f3d11ab53211d437ded738bb1a00d29b11a16aed8167518c9aa982a9084df`。
  本文旧版本 DLL 的函数地址不能直接套用到该文件；必须重新定位当前版本的
  “内部实时对象 -> OEM_REPORT”转换器后，才能继续判定公共 `amount` 的生成逻辑。

## 当前未确认

- `Ask()` 输入请求串如何映射为本地 `2000` 桥包，以及这些对象再如何流转为上游 `6100/7100` 会话负载
- 是否存在私有握手包
- `网络包` 外层记录里各整数位的精确定义
  - 当前只确认了保守层面的长度/常量提示：
    - `field32 / field36` = 重复包长
    - `field40` = 当前样本恒为 `2`
    - `field44 / field56` 更像 `payload_len_hint / object_span_len_hint`
    - `field52` 仍只能先当 `attr_flags_hint`
- `7100` 登录之后那几包 `penc / ZSTD字典 / Tdx_Encrypt` 的真实编码方式
- `penc` 和 `Tdx_Encrypt` 在 DLL 内部的确切调用链与参数
- `网络包.field44 = 12` 这一类包里，`数据` 字段的真实封装格式
- `183.236.97.134:14017` 这组随机二进制流与当前 `网络包` 协议是否相关

## 本地逆向证据

- 本节默认指的是仓库参考样本里的 `Stock64.dll` 逆向结果。
  这和 `windows_debug` 里现场主用的 `x86 Stock.dll` 不是同一份二进制，
  但当前对象模型、字段名与发包路径分析仍可作为高价值旁证，用来和现场 `Stock.dll` 行为做交叉校对。

- `Stock64.dll` 只导出：
  - `Start`
  - `Ask`
  - `Stop`

- 这三个导出地址分别是：
  - `Start = 0x180016330`
  - `Ask = 0x180016480`
  - `Stop = 0x1800163c0`

- 三个导出都落在同一个共享大函数里：
  - `0x180015d40 .. 0x180016bd2`
  - 也就是说 DLL 不是“3 个完全独立的小函数”，而是“共享栈框架 + 多入口标签”

- `Ask` 导出内部会解析查询参数，而不是把请求串直接透传。
  已从反汇编中看到对 `等待` 和 `&` 分隔符的处理。

- `Ask` 内部已经能确认会按 `请求` 字段做中文字符串分发。
  当前已定位到这些请求名：
  - `代码表`
  - `实时数据`
  - `分笔`
  - `分时`
  - `除权`
  - `财务`
  - `F10资料`
  - `登录`

- `Ask` 里还可见两个特殊分支：
  - `登录测试`
    - 会返回本地 HTML 表单页，而不是直连行情服务器
  - `重启电脑`
    - 会检查 `账号=Nezip` 和 `密码=restart`

- DLL 中可见以下网络相关字符串：
  - `套接字`
  - `外网地址`
  - `本地地址`
  - `主端口`
  - `监听端口`
  - `网络包`
  - `数据`
  - `应答编号`
  - `压缩`
  - `上传文件`

- DLL 二进制里没有直接出现 `股票数据?` 这个 UTF-16 字面量。
  这和前面的结论一致：
  - `Ask()` 会先解析本地请求串
  - 再按内部对象模型重组
  - 然后进入本地桥和/或上游发送路径
  - 而不是把 `股票数据?...` 文本原样直接透传到远端 socket

- DLL 还包含：
  - `WS2_32.dll` 导入
  - `gzip/deflate`
  - `HTTP/1.1 200 OK`
  - `Server: Nezip WebServer`

- 现在可以把 DLL 的发送路径描述得更具体一些：
  1. `Ask()` 先解析本地请求串，抽取 `请求/等待/...`
  2. 再按 `请求` 名分发到对应处理函数
  3. 业务对象会被包装进 `网络包.数据`
  4. 可选写入 `原长度/哈希值/属性/压缩`
  5. 最终序列化成 TCP 字节流并进入 socket 发送缓冲区（Windows 现场主链先进入 `127.0.0.1:2000` 本地桥）

- 结合完整抓包，现在可以把“首包”再精确一层：
  1. 客户端先向两个认证服务器各发一条完全相同的 `测速` 请求
  2. 这条请求会先被包装成里层 `认证` 对象
  3. 再被 `ZSTD` 压缩写入外层 `网络包.数据`
  4. 在当前抓到的上游会话里，这个对象最终出现在发往 `6100` 的包里
  5. 所以当前完整首包并不是登录行情或初始化分析软件，而是认证服务器测速阶段

- `Stock.dat` 这条 `32-bit` 分支里还能直接读到 `CTdxClient` 的 UTF-16 JSON 模板串，已经能和 `7709` 的前置链语义对上：
  - `0x102058` 一簇：
    - `%s(股票备用)测速包`
    - `申请服务器信息`
  - `0x102350` 一簇：
    - `登记客户端版本号，获得通行证信息`
    - `网络验证成功`
    - `正在读取代码表`
  - `0x102830` 一簇：
    - `验证用户合法性`
  - 这说明当前样本里至少可以把以下实际网络行为和 DLL 内部语义对应起来：
    - `12` 字节探活流
    - `347` 字节 bootstrap
    - `tdxlevel2` 客户端版本登记/通行证阶段
    - 后续 `0x0450` 代码表块同步
  - 同一批模板附近还能看到：
    - `CTdxClient::Task(CRcvServer *rcvS)`
    - `CTdxClient_GetPack_非法数据包`
    - `从认证服务器获取股票登录信息出错`

- `dll_data_xrefs` 现在已经支持 `PE32` 的常见绝对地址引用（如 `push imm32` / `mov reg, imm32` / `lea reg, [abs32]`），因此 `Stock.dat` 里的 `CTdxClient` 模板已经能反查到真实代码位置：
  - `tdxlevel2 @ 0x1010357c`
    - `xrefs: 0x1005de50 push-imm32`
  - `.//DzhHqServer.ini @ 0x10100c84`
    - `xrefs: 0x100396cf push-imm32`

- 继续顺 `Stock.dat` 的 `CTdxClient` 分支逆下来，当前已经能把 `7709` 的三段 bootstrap 和具体 caller 对上：
  - 第一段 `292` 字节首帧：
    - caller: `0x1005ce67`
    - 先 `push 0x7b000b`
    - 调 `0x10066c90`
    - 随后调 `0x10066b60`
    - 再调 `0x1007b7c0`
    - 当前能确认这条分支在组首个 `0x010c / 0x7b00 / ... / 0x000b` 包体
  - 第二段 `13` 字节小帧：
    - caller: `0x1005db3f`
    - `push 0x94000d`
    - 调 `0x10066c90`
    - 对应第二个 `0x020c / 0x9400 / ... / 0x000d`
  - 第三段 `42` 字节 `tdxlevel2` 帧：
    - caller: `0x1005de15`
    - `push 0x990fdb`
    - 调 `0x10066c90`
    - 在 `0x1005de50` push `tdxlevel2`
    - 对应第三个 `0x030c / 0x9900 / ... / 0x0fdb`

- 其中 `0x10066c90` 的语义已经基本明确：
  - 它会先清零一个 `12` 字节小头
  - 再把调用参数写成：
    - `byte[0] = arg5`
    - `word[3] = hi16(arg1)`
    - `byte[5] = arg2`
    - `word[6] = arg3 + 2`
    - `word[8] = arg4 + 2`
    - `word[10] = lo16(arg1)`
  - 因此它更像“内部 client10 小头模板构造器”，而不是最终线上字节流的唯一来源

- 真正把这些小结构变成线上 `client10` 字节流的是 `0x10064c40`：
  - 它会在发送阶段调用：
    - `0x10066480`
      - 返回值会写到 `frame+1`
      - 结合线上样本，当前强烈怀疑这就是包序号/会话序号来源
    - `0x100665c0`
      - 返回 `word[6] + 0x0a`
      - 对应线上总长度
  - 这解释了为什么：
    - 第一帧线上看到的是 `0c 01 00 7b 00 01 1a 01 1a 01 0b 00`
    - 第二帧是 `0c 02 00 94 00 01 03 00 03 00 0d 00`
    - 第三帧是 `0c 03 00 99 00 01 20 00 20 00 db 0f ...`
  - 也就是说，线上 `0x010c / 0x020c / 0x030c` 的高字节不是 `0x10066c90` 静态写死的，而是发送时按 session 回填出来的

- 这轮把几个之前“只看过 VA、没确认原始字节”的常量也对实了：
  - `0x10115218 @ raw 0x114018`
    - 实际字节：`d6 d0 d0 c5 d6 a4 c8 af 00 00 00`
    - `GBK` 解码后就是 `中信证券`
    - 唯一代码引用：`0x10066b8f push 0x10115218`
  - `0x10115224 @ raw 0x114024`
    - 实际字节：`4e 65 74 43 61 72 64 4d 61 63 00 00`
    - ASCII `NetCardMac`
  - `0x10103258`
    - UTF-16 `Tdx_Encrypt`
    - 由 `0x1005cea9` push，直接进入 `0x1007b7c0`

- 因此，`0x10066b60` 的语义现在可以写得更明确：
  - 先 `memset(dst, 0, 0x118)`
  - `dst + 0x33` 拷 `0x32` 字节第二个参数
  - `dst + 0x65` 固定拷 `11` 字节的 `中信证券`
  - `dst + 0xb1` 通过 `0x10009480` 拷第三个参数
  - 比较第一个参数是否等于 `NetCardMac`
    - 若相等，则把 `dst + 0xb1` 再拷到 `dst + 1`
    - 否则把第一个参数直接拷到 `dst + 1`
  - 还会写固定字段：
    - `dst[0x70] = 0x07`
    - `dst[0x71] = 0x00`
    - `dst[0x76..0x79] = 8.56f`
    - `dst[0x7a..0x7d] = 9.99f`
    - `dst[0x7e] = 0x01`

- 这也把首个 `292` 字节首帧的调用链再收紧了一步：
  - `0x1005cea4 -> 0x10066b60`
    - 先组一个 `0x118` 字节明文 body
  - `0x1005cea9 push 0x10103258`
    - 也就是 `Tdx_Encrypt`
  - `0x1005cec2 -> 0x1007b7c0`
  - `0x1007b7c0 -> 0x1002bf90`
    - `0x1002bf90` 内部会先调 `0x1002da10`
    - `0x1002da10` 会初始化一个 `0x1414` 字节上下文，并把算法名 `Tdx_Encrypt` 放进去
  - 这说明首帧不是“`0x10066b60` 产出的明文直接喂给 `0x10064c40`”，而是“先经 `Tdx_Encrypt` 封装，再由 `0x10064c40` 发出去”

- 抓包侧也能和这条 `Tdx_Encrypt` 路径对上：
  - `7709` 首帧线上 `payload = 282 = 2 + 0x118`
    - 其中前 `2` 字节是 `tag = 0x000b`
    - 后面 `0x118` 字节是高熵 body
  - 当前看到的高熵 body 里有大量重复的 `8` 字节块，例如：
    - `74 99 33 ae 27 70 03 57`
  - 强推断：
    - 这更像“`0x118` 明文结构经过固定 `8` 字节块处理后的结果”
    - 不是 `0x10066b60` 的明文字段直接裸发
    - 目前还不能仅凭这些重复块断定它就是简单 XOR；更保守的表述是“存在 `8` 字节粒度的变换/加密迹象”

- `Tdx_Encrypt` 不是只服务这一条首帧：
  - 另一个直接 caller 是 `0x1006daa7`
    - 它先 `0x10066c90(0x26ba, len=0x50)`
    - 再 `0x1006dfa0`
    - 然后同样 `push 0x10104a64 (Tdx_Encrypt)` 进入 `0x1007b7c0`
  - `0x1006dfa0` 本身会往 `0x50` 小结构里写：
    - `tdxlevel2`
    - `%02X`
    - 以及另一段通过 `0x10009480` 写入的字符串
  - 所以 `0x1007b7c0 / 0x1002bf90` 更像是通用“加密后发送”包装层，而不是某个单独业务包的特例

- 首帧 builder 的三个输入参数，这轮也已经能从上游代码里落到具体语义：
  - `ctx + 0x6490f4`
    - 不是抽象 token
    - `0x1004f5d0` 会在选中当前服务器对象后调用 `0x10049530`
    - `0x10049530` 的格式串已经确认是：
      - `"%d.%d.%d.%d"`
      - `"%d.%d.%d.%d:%d"`
    - 所以 `0x6490f4` 就是“当前服务器 IPv4 文本（必要时带端口）”
  - `ctx->6490e8 + 0xc4`
    - 不是地址
    - `0x1002e940` 里它会被代入
      - `&account=%s&password=%s&version=%d&localIp=%u&volumeId=%u`
    - 因此可以把 `+0xc4` 明确记成账号字符串槽
  - `ctx->6490e8 + 0xe4`
    - 同理是密码字符串槽

- 于是 `0x1005ce72 -> 0x10066b60` 的三参数关系现在可以写成：
  - `arg1 = 当前服务器对象.account`
  - `arg2 = 当前服务器对象.password`
  - `arg3 = 当前服务器 IPv4 文本`
  - 函数内部仍保留一个特殊分支：
    - 若 `account == "NetCardMac"`，则 `dst + 1` 不直接拷账号，而改用第三个参数
    - 这说明 `NetCardMac` 更像一个占位符/特殊模式，而不是普通账号值

- 这也让首帧后的状态机更清晰了：
  - 在首个 `292` 字节 `0x010c / 0x7b00` 包发完之后，`0x1005cf60` 会立刻调用 `0x1004fce0`
  - 随后又调 `0x1004fe40`
  - 成功时：
    - 继续初始化/筛选后续连接项
    - 再算一个 `ctx + 0x8c` 相关值写到 `ctx + 0x1b4`
  - 失败时：
    - 回落到 `0x1004f690` 的错误日志路径
  - 这说明首帧不是终点，而是一个“拿到当前服务器对象后，继续派生/筛选后续连接”的 bootstrap 起点

- 同一份 `Stock.dat` 里还存在明显独立的 `CDzhServer` / `大智慧服务器` 分支：
  - 例如 `0xffa84` 附近可见：
    - `.//DzhHqServer.ini`
    - `大智慧服务器`
    - `分析软件路径`
    - `股票数据?类型=%s&代码=%s&模块=股票备用`
    - `大智慧新一代接收系统V6.0 Build ...`
  - 这条分支说明当前 DLL/壳同时兼容另一套“大智慧”客户端协作路径；它和 `7709` 的 `CTdxClient` 逻辑并存，但暂时还不能证明 `7708/7719` 就直接走了这条分支

- 这轮又把 `0x1004fce0 / 0x1004fe40` 的网络语义坐实了：
  - 新增的 `dll_imports` 工具可以直接列出 IAT 地址：
    - `cargo run --example dll_imports -- ./netzip_api_bin/NetzipAPI/StockC#/Stock.dat 0x100f22c8 0x100f22cc 0x100f22d0 0x100f22ec 0x100f22f0`
  - 已确认：
    - `0x100f22c8 = WSACreateEvent`
    - `0x100f22cc = WSAEventSelect`
    - `0x100f22d0 = WSACloseEvent`
    - `0x100f22ec = WSAWaitForMultipleEvents`
    - `0x100f22f0 = WSAEnumNetworkEvents`
  - 所以 `0x1004fce0` 可以更准确地描述为：
    - 用 `0x100495e0(this, source, 0)` 拿到 socket
    - 在 `ctx + 0x254 + (base_slot + i) * 0x1016c` 构造连接项
    - 把 `source + 0x404` 这段 `16` 字节地址结构复制到 `entry + 0x8c`
    - 把 `source + 0x414` 这段 `16` 字节地址结构复制到 `entry + 0x40`
    - `entry + 0x15c = source`
    - 创建 `WSAEVENT`
    - 绑定 `WSAEventSelect(socket, event, 0x30)`
    - 然后对 `source + 0x414` 发起 `connect`
  - `0x1004fe40` 则会：
    - 把候选 `event` 收集成数组
    - 用 `WSAWaitForMultipleEvents(..., timeout=0x1f4)` 轮询
    - 用 `WSAEnumNetworkEvents(socket, event, &events)` 判断 `FD_CONNECT/FD_CLOSE`
    - 在总超时约 `0x7d0 = 2000ms` 内，把成功/失败候选分流
    - 最终把可用候选压到 `ctx + 0x6490a0`，再由上层通过虚表 `+0x50` 选出 `ctx + 0x6490e8`

- `0x1005d050` 这条登录 reply 状态机，现在已经能顺到“开始拉代码表”为止：
  - `0x7b000b` 回复 -> `0x1005db14`
    - 直接调用 `0x10066c90(0x94000d)` 组第二包
    - 模板串已确认：
      - `{"信息" : "申请服务器信息", "服务器" : "%s", "应答" : "登录", ...}`
  - `0x94000d` 回复 -> `0x1005dc47`
    - 条件分支只在 `entry->0x78 == 1` 这类登录主链上继续
    - 把当前服务端返回块复制到 `ctx + 0x64fc54`
      - 复制长度是 `0x2f * 4 + 1 = 0xbd` 字节
    - 把当前服务器对象整块快照复制到 `ctx + 0x64f810`
      - 复制长度是 `0x111 * 4 = 0x444` 字节
    - 顺带更新：
      - `ctx + 0x64f80c`
      - `ctx + 0x64d210`
    - 随后进入 `0x1005de15`
      - 先 `0x10066c90(0x990fdb)`
      - 再 `0x10066700 -> 0x10066a30`
      - 最后 `0x10064c40` 发第三包
    - 相关模板串已确认：
      - `{"信息" : "登记客户端版本号，获得通行证信息", "服务器" : "%s", "应答" : "登录", ...}`
  - `0x990fdb` 回复 -> `0x1005df49`
    - 成功路径会先记录：
      - `{"信息" : "网络验证成功", "说明" : "通过网络验证", "提示" : "%s", ...}`
    - 然后立刻调 `0x100618f0`
      - 它按 `ctx + 0x64d4bc` 选择：
        - `0x6d0450`
        - 或 `0x6e0450`
      - 发送模板是：
        - `{"信息" : "正在读取代码表", "服务器" : "%s", "应答" : "登录", ...}`

- `0x100618f0 / 0x10061a00` 这对函数，已经把“登录后读取代码表”的格式钉住了：
  - `0x100618f0`
    - 把当前 bucket 编号写进请求体的 `+0xc/+0xe`
    - 然后发 `0x6d0450 / 0x6e0450`
  - `0x10061a00`
    - 以 `u16 count` 开头读取服务端回复
    - 后续每条记录固定 `0x1d = 29` 字节
    - 这与 Linux 侧已经复现成功的 `7709` 代码表记录长度完全一致
    - 循环里会把 `6` 字节代码、`8` 字节名称以及若干字段整理后插入 `ctx + 0x64f798`
  - 收尾规则：
    - 若本次 `count < 1000`，则重置 `ctx + 0x64d4c0 = 0`，并把 `ctx + 0x64d4bc += 1`
    - 若 `ctx + 0x64d4bc` 追到 `ctx + 0x64d4b8` 上限，则置为 `-1`
    - 因而 `0x100618f0` 返回 `0` 并不表示“发包失败”，而是“代码表所有 bucket 已经取完”
    - 此时 `0x1005e0fc` 会转入 `0x10061bd0` 做代码表同步完成后的收尾

- 这个闭环让 `Stock.dat` 的登录 bootstrap 现在可以按时间顺序写成：
  - 选出当前服务器对象 `ctx + 0x6490e8`
  - 发送首个 `292` 字节 `0x010c / 0x7b000b`
  - 收到首包回复后发送 `0x94000d`
  - 收到第二包回复后发送 `0x990fdb`
  - 收到第三包回复后开始循环发送 `0x6d0450 / 0x6e0450`
  - 把 `29` 字节记录代码表完整拉回
  - 再进入代码表完成后的后续阶段

- 代码表完成后的 post-login 链，这轮又往下推进了 3 段，而且已经能和此前抓到的 `0x0547` 业务流对上：
  - `0x10061bd0`
    - 会遍历 `ctx + 0x64f798` 里的代码表对象
    - 按 `item->0x14 == 1 / 0x10` 分到两个临时集合
    - 再去重、整理后压回 `ctx + 0x64f700 / 0x64f748` 这套后续业务容器
  - `0x100653c0`
    - 现在已经确认是“市场前缀 + 6 字节代码”的转换器
    - `flag = 0 -> "SZ"`
    - `flag = 1 -> "SH"`
    - 即后续几个请求里反复出现的 `7` 字节 tuple 可以直接写成：
      - `u8 market_flag`
      - `char code[6]`
  - `0x10062340 -> 0x100625c0`
    - `0x10062340` 会从 `ctx + 0x64f748` 取条目，最多打包 `50` 个
    - 请求体结构已经能明确写成：
      - `u16 count`
      - `count * 7-byte tuple`
    - 组包常量是：
      - `0x76000f`
    - `0x100625c0` 则负责解回包：
      - 外层是 `u16 group_count`
      - 每组有 `9` 字节头
      - 每股固定 `0x1d = 29` 字节
    - 全部页都拉完后，`0x100625c0` 会递归回 `0x10062340`；当它返回 `0` 时，才进入下一阶段 `0x10062830`
  - `0x10062830 -> 0x10062ab0`
    - 仍然使用同样的 `7` 字节股票 tuple
    - 但这轮每次最多只取 `20` 个
    - 组包常量变成：
      - `0x750010`
    - `0x10062ab0` 对应的回包结构也已经能约束住：
      - 开头仍是 `u16 count`
      - 每条记录固定跨度是 `0x8f = 143` 字节
    - 这条 `20只 -> 143字节` 约束现在已经被真实 `7709` 长会话印证：
      - 代表流 client `frame[511..594]` 连发 `84` 个 `sub=0x7502` 请求，payload 基本都是 `144`
      - 最后 client `frame[595]` 是收尾请求，payload=`67`
      - 这和 `u16 count + count * 7` 完全对得上：
        - `144 = 2 + 20 * 7`
        - `67 = 2 + 9 * 7`
      - 对应 server `frame[511..594]` 也正好是 `84` 个 `sub=0x7502 / tag=0x0010` 回复
      - 其 `original` 基本都固定为 `2862`
        - 也就是 `2 + 20 * 143`
      - 最后 server `frame[595]` 的 `original=1289`
        - 也就是 `2 + 9 * 143`
    - 全部页都跑完后，会清理 `1012a970 / 1012a96c` 这两套全局容器，并调用：
      - `0x1007d6b0`
      - `0x1007e710`
    - 这两个函数挂的模板串已经解到：
      - `股票数据`
      - `除权财务`
      - `数据接口`
    - 所以它们更像公共结果分发/通知层，而不是新的 socket 登录入口
    - 进一步拆下去后，`0x1007d6b0 / 0x1007e710` 的具体落点也已经更明确：
      - `0x1007d6b0 -> 0x1007de80`
        - 物化的是 `除权数据`
        - 单条输出记录固定 `0xc8 = 200` 字节
        - 会按 `SH / SZ / BJ` 三种文本前缀去匹配 `1012a970` 里的 `0x78` 字节中间记录
      - `0x1007e710 -> 0x1007e210`
        - 物化的是 `财务数据`
        - 单条输出记录固定 `0x15e = 350` 字节
        - 会按同样的 `SH / SZ / BJ` 前缀去匹配 `1012a96c`
      - 附近的兄弟函数 `0x1007e4d0`
        - 模板串是 `实时数据`
        - 单条输出记录固定 `0x1f4 = 500` 字节
      - 这进一步说明这一层是共享的数据对象组装/分发层，不是网络协议本身
  - `0x10062c40`
    - 调用点已经确认在 worker 侧 `0x1005ec35`
    - 它会遍历当前活跃连接项 `ctx + 0x254 + i * 0x1016c`
    - 对每个 `entry + 0x80 != 0` 的连接，尝试发送一批初始化行情请求
    - 这批请求每项不再是 `7` 字节，而是：
      - `7-byte tuple`
      - `u32 token = item + 0x1e6 + 0xd4`
    - 也就是 `11` 字节一项，单包最多 `100` 项
    - 对应的组包常量是：
      - `0x290547`
    - 发完后会把 `entry + 0x120 = now`
  - `0x100631e0`
    - 调用点已经确认在 `0x1005d85e`
    - 它是 `0x0547` 这条链上的 reply dispatcher
    - 先统一把 payload 丢给 `0x100632b0`
    - 如果当前 opcode 正好是 `0x290547` 且 payload 长度大于 `0x2b`，则继续触发 `0x10062f00`
    - 否则也会先把 `entry + 0x110 = 0`，再进入 `0x10062f00`
  - `0x10062f00`
    - 现在已经能明确是 `0x290547` 之后的增量/续订发送器，而不是另一个独立 bootstrap
    - 它仍然从 `ctx + 0x64f700` 枚举股票
    - 但会根据每股附加结构 `item + 0x1e6` 上的两个时间戳做筛选：
      - `now - (item + 0x1e6 + 0xd0) >= 0x7d0`
      - `now - (item + 0x1e6 + 0xcc) >= 0x320`
    - 命中的条目会再次组 `11` 字节一项的请求，单包最多 `100` 项
    - 对应的组包常量是：
      - `0x2a0547`
    - 发出后会把：
      - `item + 0x1e6 + 0xcc = now`
      - `entry + 0x110 = 1`
  - `0x100632b0`
    - 这条 parser 也已经拿到硬约束：
      - 回包首个 `u16 count` 必须 `<= 100`
      - 每条记录先带一段 `9` 字节代码头
      - 再跟着若干变长数值字段
    - 它会先用 `0x100653c0` 把 `market_flag + code[6]` 还原成 `SZxxxxxx / SHxxxxxx`
    - 然后与本地容器里现有股票对象做 key 匹配
    - 再连续解析 `5` 个字段并写回对象附加结构
    - 这次最关键的新桥接点是：
      - parser 末尾会把“续订所需的 token 和时间戳”直接写回股票对象
      - 已确认：
        - `ext + 0x2b2 = now`
        - `stock + 0xcc = now`
        - `ext + 0x2b6 = now`
        - `stock + 0xd0 = now`
        - `ext + 0x2ba = parsed_record + 0x21`
        - `stock + 0xd4 = ext + 0x2ba`
      - 这正好解释了为什么 `0x10062f00` 后面会用：
        - `now - (item + 0x1e6 + 0xd0) >= 0x7d0`
        - `now - (item + 0x1e6 + 0xcc) >= 0x320`
      - 以及 `item + 0x1e6 + 0xd4` 作为下一轮 `0x2a0547` 的 `u32 token`
      - 换句话说，`0x100632b0` 已经把“回包 -> 对象状态 -> 下一轮续订请求”这条闭环打通了
    - 此外，`0x100632b0` 中那段 `5 * 4` 数值块的分支条件也已经拆开：
      - 它会调用 `0x10064300(code, market_flag)`
      - `market_flag = 0` 时，仅当代码以 `39` 开头返回真
      - `market_flag = 1` 时，当代码以 `8` 或 `00` 开头返回真
      - 真分支会把两列价格直接按 `0.001` 比例缩放写入
      - 假分支则会在两列价格上都加上基准价 `base = parsed_record + 0x1d`
      - 因而这更像“特殊代码类别选择不同五档价格编码方式”的分类器，而不是普通权限判断

- `实时数据` 这一层，这轮已经基本钉死成 `OEM_REPORT(pack=1)` 映射，不再只是“像 500 字节对象”：
  - 本节地址来自当时分析的 DLL 样本，是历史结构证据。当前生产公共时间行为以
    前述 `网际风.exe 0x4839a3 -> 0x438800` 为准，不能把下列地址直接套到当前 DLL。
  - 头文件 [OemStock.h](/home/codes/netzipapi-rust-demo/netzip_api_bin/NetzipAPI/StockC++/OemStock.h) 已确认有：
    - `#pragma pack(push, 1)`
    - `struct OEM_REPORT // 实时数据，500 字节`
  - `0x10083980`
    - 只是对输出缓存做 `memset(0, 0x1f4)`
    - 这正好等于 `500` 字节
  - `0x1007e4d0`
    - 会枚举内部实时数据容器
    - 为每条分配 `0x1f4` 字节输出记录
    - 再逐条调用 `0x1007ef80`
  - `0x1007ef80`
    - 现在已经能和 `OEM_REPORT(pack=1)` 做偏移级一一对应
    - 已确认的关键映射：
      - `+0x058 = time`
      - `+0x05c = foot`
      - `+0x060 = openDate`
      - `+0x064 = openTime`
      - `+0x068 = closeDate`
      - `+0x06c = open`
      - `+0x070 = high`
      - `+0x074 = low`
      - `+0x078 = close`
      - `+0x07c = volume`
      - `+0x080 = amount`
      - `+0x084 = inVol`
      - `+0x088 = pricesell[5]`
      - `+0x0b0 = volsell[5]`
      - `+0x0d8 = vsellCha[5]`
      - `+0x100 = pricebuy[5]`
      - `+0x128 = volbuy[5]`
      - `+0x150 = vbuyCha[5]`
      - `+0x178 = jingJia`
      - `+0x179 = avPrice`
      - `+0x17d = isBuy`
      - `+0x17e = nowv`
      - `+0x182 = nowa`
      - `+0x186 = change`
      - `+0x18a = weiBi`
      - `+0x18e = liangBi`
      - `+0x196 = last`
      - `+0x19a = limitUp`
      - `+0x19e = limitDown`
      - `+0x1a2 = isIndex`
      - `+0x1a3 = isDaPan`
      - `+0x1a4 = isStock`
      - `+0x1a5 = bsNum`
      - `+0x1a6 = tickNum`
    - 其中 `+0x088/+0x0b0/+0x0d8/+0x100/+0x128/+0x150` 这六段数组，正好对应头文件里的买卖价/量/变化数组
    - 这一轮再往下追 getter 后，内部实时对象的来源也基本清楚了：
      - `0x10005110 -> time = this + 0x1f2`
      - `0x10005560 -> open = (this + 0x292) / scale`
      - `0x10005590 -> high = (this + 0x1f6) / scale`
      - `0x100055c0 -> low = (this + 0x1fa) / scale`
      - `0x100055f0 -> close = (this + 0x1fe) / scale`
      - `0x10005650 -> volume = this + 0x206`
      - `0x100056b0 -> amount = helper(this + 0x1e6)`
      - `0x10005620 -> avPrice = (this + 0x2d6) / scale`
      - `0x10005690 -> tickNum = this + 0x2e2`
      - `0x100056d0 -> pricesell[i] = (this + 0x232 + i*4) / scale`
      - `0x10005710 -> volsell[i] = this + 0x272 + i*4`
      - `0x10005740 -> pricebuy[i] = (this + 0x212 + (7-i)*4) / scale`
      - `0x10005780 -> volbuy[i] = this + 0x252 + (7-i)*4`
      - `vsellCha[i]` 直接取自 `this + 0x19f + i*4`
      - `vbuyCha[i]` 直接取自 `this + 0x18b + i*4`
      - `jingJia` 取自 `this + 0x2ab` 的 bit0
      - `isBuy / nowv / nowa` 分别来自 `this + 0x1bb / 0x1b3 / 0x1b7`
      - `last / limitUp / limitDown` 分别来自 getter `0x10005130 / 0x10005160 / 0x100051c0`
      - `isIndex / isDaPan / isStock / bsNum` 分别来自 `this + 0xb0 / 0xb2 / 0xb1 / 0xb5`
    - 这里还有一个容易忽略的细节：
      - [OemStock.h](/home/codes/netzipapi-rust-demo/netzip_api_bin/NetzipAPI/StockC++/OemStock.h) 里这几组盘口数组声明成了 `float [10]`
      - 但 `0x1007ef80` 当前只显式循环填了前 `5` 档
      - 这和注释里的“申卖价1..5 / 申买价1..5”是一致的，只是结构体槽位预留得更大
  - 这意味着：
    - `0x1007ef80` 本质上就是“内部实时数据对象 -> DLL 对外 `OEM_REPORT` 输出结构”的拷贝/转换器
    - 而 `0x1007e4d0` 则是批量包装 `实时数据` 返回结果的外层枚举器
  - 另外，`0x1007e4d0` 的上游 caller `0x10076190 / 0x10076467 / 0x10076521 / 0x100785f1 / 0x100816cf` 周围都能看到 `股票数据 / 除权财务 / 数据接口`、`补分笔 / 补K线 / 补F10` 这组模板串
    - 这进一步说明它属于 API 导出/回填层，而不是新的网络登录分支

- `0x10025f30(field_id)` 这一层，这轮已经把 `OEM_REPORT` 里几个“看名字像行情、实际上是派生指标”的字段压实了：
  - 这是一个通用 `field_id -> 取值公式` 分发表
  - 已直接确认：
    - `field_id = 0x0b`
      - 跳到 `0x100262aa`
      - 最终写进 `OEM_REPORT.change`
      - 这不是“涨跌额/涨跌幅”，而是头文件注释里的 `换手率`
      - 证据是这条分支会按市场类型改用不同分母字段，再用当前 `volume` 去做比值
    - `field_id = 0x15`
      - 跳到 `0x100260c9`
      - 最终写进 `OEM_REPORT.weiBi`
      - 公式已经清楚：
        - `weiBi = (委买 - 委卖) / (委买 + 委卖) * 100`
        - 其中委买/委卖分别来自内部对象 `0x1c4 / 0x1c8`
    - `field_id = 0x17`
      - 跳到 `0x100260ff -> 0x100059c0`
      - 最终写进 `OEM_REPORT.liangBi`
      - 公式更像“当前成交量按已过分钟数折算后，与平均量做比值”
      - 具体写法是：
        - `volume * (slot_0xb6 + 1) / (slot_0x12e * (foot + 1))`
      - 这和 `量比` 的语义是一致的

- `0x1007d810` 这条“初始化数据”分支，这轮也已经基本坐实为 `OEM_STKINFO(250B)` 导出器：
  - 现在更准确地说，是 `OEM_MARKETINFO + OEM_STKINFO[]` 的代码表初始化导出器：
    - `0x10083a00(base) = base + 0xc8`
    - `0x100839c0(n) = 0xc8 + n * 0xfa`
    - [OemStock.h](/home/codes/netzipapi-rust-demo/netzip_api_bin/NetzipAPI/StockC++/OemStock.h) 里：
      - `offsetof(OEM_MARKETINFO, stkInfo) = 0xc8`
      - `sizeof(OEM_STKINFO) = 0xfa = 250`
  - 几个关键字段已经能对上：
    - `base + 0x00 = mkId`
    - `base + 0x02 = name`
    - `base + 0x42 = tmCount`
    - `base + 0x44 = openTime[8]`
    - `base + 0x54 = closeTime[8]`
    - `base + 0x64 = date`
    - `base + 0x68 = num`
    - 通过 `0x10006760(..., 0x0c)` / `0x10006760(..., 0x20)` 拷入 `label / name`
    - 从对象 `+0xe6 .. +0x116` 区间拷入 `openTime / closeTime`
    - `0x10005130 / 0x10005160 / 0x100051c0` 分别填到 `last / limitUp / limitDown`
    - `0xb0 / 0xb2 / 0xb1 / 0xb5` 分别落到 `isIndex / isDaPan / isStock / bsNum`
  - 它附近还直接出现了：
    - `%s代码表`
    - `股票代码日期`
    - `期货代码日期`
    - `除权数据 / 财务数据 / 实时数据`
  - 结合 caller `0x10075fc7 / 0x10076351 / 0x10077f04`
    - 现在更像“初始化阶段按市场/分页导出代码表，再接除权/财务”的那条 API 输出链

- `除权 / 财务` 这两条导出链，这轮也已经能和头文件尺寸完全扣上：
  - `0x1007de80`
    - 分配大小是 `0x3e8 + count * 0xc8`
    - 尾部会用 `(payload_len - 0xc8) / 0xc8` 回算条数
    - 这和 [OemStock.h](/home/codes/netzipapi-rust-demo/netzip_api_bin/NetzipAPI/StockC++/OemStock.h) 里：
      - `OEM_SPLIT_HEAD = 0xc8 = 200`
      - `OEM_SPLIT = 0xc8 = 200`
      - 完全一致
    - 因而这条链基本可以视为 `OEM_SPLIT_HEAD + OEM_SPLIT[]` 的组包器
    - 这轮再往里拆后，单条 `OEM_SPLIT` 的写法也已经非常清楚：
      - 原始记录来自 `0x10017e20`，按 `count * 0x78` 取出
      - `0x1007de80` 对每条先把前 `0x14` 字节直接拷到目标记录开头
        - 这正好对应 `time + give + allocate + price + earnings`
      - 然后把原始记录 `+0x14` 开始的 `0x28` 字节说明文本，经 `0x100075f0` 转成宽字符，写到目标记录 `+0x14`
        - 这正好对应 `WCHAR explain[90]`
      - 换句话说，`OEM_SPLIT` 这条已经从“尺寸对上”推进到了“字段布局对上”
  - `0x1007e210`
    - 分配大小是 `0x3e8 + count * 0x15e`
    - 头文件里 `OEM_FINANCE` 明确是 `350字节`
    - `0x15e = 350`
    - 所以这条链就是 `OEM_FINANCE[]` 的财务导出器
    - 当前至少还能确认它的组包分段：
      - `label` 先从内部对象 `+0x162` 拷到目标偏移 `0x00`
      - `name` 再从内部对象 `+0x18` 拷到目标偏移 `0x18`
      - 随后有一段 `0xcc = 204` 字节的连续块拷到目标偏移 `0x58`
      - 而 `OEM_FINANCE` 从偏移 `0x58` 开始，刚好是：
        - `time + baoGao + shangShi`
        - 再加后面全部 float 字段
      - 更细一点看，`0x1007e210` 实际上是先让 `0x10018920` 生成一块 `0xe0` 字节中间缓冲
        - 然后跳过它前 `0x0c` 字节
        - 只把后面的 `0xcc` 字节搬到 `OEM_FINANCE + 0x58`
      - 这轮把这块中间缓冲背后的对象池也对上了：
        - `0x100198d0 / 0x10019c00` 管的是 `0xe4` 字节槽位
        - `0x10019db0` 只清前 `0xe0` 字节，不碰 `obj + 0xe0`
        - `0x1001995f` 会把 `obj + 0xe0` 当成 free-list next 指针复用
        - `0x1000fe20` 往有序索引项里写入的是对象基址本身，不是 `obj + 4`
        - `0x1000ef60` 再按代码字符串二分查找，并把这个对象基址原样取回
      - 这也解释了为什么 `skip 0x0c` 不是偶然：
        - `0x10018920` 拷出来的是对象基址开始的前 `0xe0` 字节
        - `0x1007e210` 固定从 `obj + 0x0c` 开始贴到 `OEM_FINANCE + 0x58`
        - 因而可以把 `obj + 0x0c / +0x10 / +0x14` 直接视为 `time / baoGao / shangShi`
        - 后面的 `obj + 0x18 .. +0xd7` 就是全部财务 float 字段的连续 payload
        - 更准确地说，被跳过的 `obj + 0x00 .. +0x0b` 更像 raw finance record 自带的 `12` 字节 code/key 槽，而不是单纯“3 个内部 DWORD”
        - 这轮还补上了来源链：
          - `0x10019710` 会用 `rep movsd` 把上游原始记录整块复制 `0xe0` 字节到对象基址
          - `0x10018478` 还会把同一个源指针同时作为 `record_src` 和 `lookup_key` 传给 `0x10019710`
          - 这说明上游原始财务记录在字节 `0` 起就带可用于查找的代码/key
          - `0x100653c0` 已确认会把 `market_flag + code[6]` 格式化成 ASCII `SZ000001 / SH600000`
          - `0x10019d20(flag = 0)` 会把这类 key 的前 `12` 字节直接拷到记录 `+4`
          - `0x10017a00` / `0x1000fe20` 再用 `record + 4` 作为索引 key 写到目录项 `entry + 8`
          - 而 `0x1000ef60` 的比较器按 `2` 字节一组扫描，天然适配这种“`8` 字节 ASCII symbol + 尾部零填充到 `12` 字节”的 key 形式
          - 因而 `skip 0x0c` 现在已经可以较强地写成：跳过前导的 `12` 字节 ASCII symbol/key 槽，再进入 `time / baoGao / shangShi + floats`
      - 另一个容易混淆的点也已经澄清：
        - `0x10019df0` 走的是另一条 `0x98` 字节线性化 helper
        - 它服务的是别的内部结构，不是 `0x1007e210` 这条 `OEM_FINANCE` 导出链
      - 同时，`0x10018b00` 现在也能确认是 `finance getter(field_id = 0x2a..0x4b)`：
        - 它会先通过 `0x1000ef60` 拿到同一块 `0xe4` 财务对象
        - 大部分 case 都是直接读取 `obj + 0x18 / 0x1c / 0x20 / ... / 0xcc`
        - 这些偏移与 `OEM_FINANCE + 0x58` 的字段序列完全同相，只是整体前移了 `0x0c`
        - `0x2a` 是目前最明确的 special case：
          - 它不是直接读 float，而是从 `obj + 0x10` 取一个 `YYYYMMDD` 风格整数
          - 按 `((value % 2000) / 100) / 3` 折成 `1..4`
          - `0` 会被回补成 `4`
          - 结合 `0x100263ab` 那条 `price / mgShouYi * quarter / 4` 的 annualize 公式，这里基本可以定性成“由 `baoGao` 推导出的报告季度号”
        - 已经能稳定对上的 direct case 例如：
          - `0x2b -> obj + 0x98 -> zongGu`
          - `0x2c -> obj + 0xa0 -> liuTongAG`
          - `0x33 -> obj + 0xa4 -> bGu`
          - `0x35 -> obj + 0x18 -> mgShouYi`
          - `0x44 -> obj + 0x1c -> mgJingZhi`
          - `0x3a / 0x3b / 0x3d / 0x3e / 0x3f / 0x40 / 0x41 / 0x42`
            - 分别落到 `zongZC / ldzc / guDingZC / wuXingZC / ldfuZhai / cqfuZhai / quanYi / zbGongJi`
          - `0x46 / 0x47 / 0x48 / 0x49 / 0x4a / 0x4b`
            - 分别落到 `shouRu / zyLiRun / yywShouZhi / zongLiRun / jingLiRun / weiFenPei`
        - 当前还能明确看到几项 getter 是直接返回 `0`：
          - `0x30`
          - `0x34`
          - `0x36`
          - `0x3c`
          - `0x43`
          - 这些更像“字段保留/当前源数据不提供”，不是读取失败
        - 其余仍未坐实的 9 个 getter 已经明确列为 gap：
          - `0x2d`
          - `0x2e`
          - `0x2f`
          - `0x31`
          - `0x32`
          - `0x37`
          - `0x38`
          - `0x39`
          - `0x45`
          - 代码里将它们单独收进 `FIN_GETTER_GAP_SPECS` / `FIN_GETTER_UNRESOLVED_IDS`
          - `TdxFinRecord::getter_value()` 对这些 id 继续返回 `None`
      - 因而目前可以比较有把握地说：
        - `0x1007e210` 已经把 `OEM_FINANCE` 的 `0x00..0x123` 有效业务字段完整组起来了
        - 末尾 `temp[58]` 没有参与有效载荷拷贝
      - 这轮还把 raw finance record 的上游来源钉到文件级了：
        - `0x10072627 / 0x100726b0 -> 0x10018240`
        - 对应的模板地址 `0x10105a14 / 0x10105a48`
        - 就是 `http://filedown.gw.com.cn/download/FIN/full_sh.FIN`
        - 和 `http://filedown.gw.com.cn/download/FIN/full_sz.FIN`
      - `0x10018240` 的解析格式也已经清楚：
        - 文件头 `DWORD` 固定校验 `0x223fd90e`
        - 后跟一个 `DWORD record_size`
        - 当前实现要求 `record_size >= 0xe0`
        - 之后按 `(payload_len - 8) / record_size` 迭代记录
        - 每条记录交给 `0x100183b0`
        - 这套格式现在已经按同一假设收进 `src/tdx_fin.rs`
          - `parse_fin_bytes / parse_fin_file` 直接按 `magic + record_size + records...` 解析
          - 单条 record 会保留 `12` 字节 key/symbol 槽和 `0xd8..record_size` 的 trailer
          - 示例 `examples/tdx_fin_dump.rs` 可以直接把 `full_sh.FIN / full_sz.FIN` 转成 `CSV`
        - 已用真实文件复核：
          - `full_sh.FIN -> record_size = 224, records = 2412, trailing = 0`
          - `full_sz.FIN -> record_size = 224, records = 3012, trailing = 0`
          - 当前两份文件里 raw record 的 `0xd8..0xe0` 都是固定 `00 00 80 3f 00 00 00 00`
      - `0x100183b0` 对单条 raw finance record 的判定也能看清：
        - `record + 0x0c` 已经被当成 `time`
        - `record + 0x98` 会参与“是否有效”的分支判断
        - 这与前面从导出层倒推的 `0x0c` 偏移结论是一致的
      - 另外，`0x10062b8f -> 0x10066d70 -> 0x100183b0` 还暴露了同格式 record 的“本地合成”链：
        - `0x10066d70` 先 `0x10019db0` 清空 `0xe0`
        - 再通过 `0x10067220 -> 0x10006fe0` 把外部 key 原样 `memcpy(12)` 到记录头
        - 随后把 `time / baoGao / shangShi` 和一批财务字段按同一 raw layout 写进去
        - 这进一步坐实了：finance raw record 的前 `12` 字节就是 key/symbol 槽，不是临时元数据
        - 当前已能直接确认的一组 raw 偏移有：
          - `+0x0c -> time`
          - `+0x10 -> baoGao`
          - `+0x14 -> shangShi`
          - `+0x18 -> mgShouYi`
          - `+0x1c -> mgJingZhi`
          - `+0x20 -> jzcsyl`
          - `+0x28 -> mggjj`
          - `+0x2c -> mgwfp`
          - `+0x30 -> gdqybl`
          - `+0x44 -> zongZC`
          - `+0x4c -> guDingZC`
          - `+0x50 -> wuXingZC`
          - `+0x60 -> quanYi`
          - `+0x64 -> zbGongJi`
          - `+0x68 -> xianJin`
          - `+0x78 -> shouRu`
          - `+0x7c -> zyLiRun`
          - `+0x84 -> tzShouYi`
          - `+0x8c -> zongLiRun`
          - `+0x90 -> jingLiRun`
          - `+0x94 -> weiFenPei`
          - `+0x98 -> zongGu`
          - `+0xa0 -> liuTongAG`
          - `+0xa4 -> bGu`
          - `+0xa8 -> jingwaiGu`
  - 这样一来，当前 DLL 导出层里已经有 4 类结构被尺寸级坐实：
    - `OEM_MARKETINFO + OEM_STKINFO[]`
    - `OEM_SPLIT_HEAD + OEM_SPLIT[]`
    - `OEM_FINANCE[]`
    - `OEM_REPORT[]`

- 强推断，但已经有较强证据支撑：
  - `0x290547 / 0x2a0547` 就是此前抓包里看到的那条 `0x0547` 行情业务链
  - 证据是：
    - 常量低 16 位都落在 `0x0547`
    - 发送侧确实在做“按股票批量订阅/续订”
    - 回包侧确实在按 `count <= 100` 的逐股结果写回本地对象
    - 与此前 `7719` 抓包里“按 80/34 只股票分帧”的现象是同一类型
  - 但目前还不能把它 100% 等同到某一条具体端口流，所以这里仍保留“强推断”标记

- 到目前为止，关于“`0x0547` 对应 `实时数据 / OEM_REPORT`”这件事，可以明确分成两层信心：
  - 已经是硬证据的部分：
    - `7709` 的完整主流会话里，`0x0547` 已被直接观测到，不再只是 DLL 静态推断
    - 同一条代表流里，`0x7600 -> 0x7502 -> 0x2802/0x2902(tag=0x0547)` 的阶段顺序已能稳定复现
    - `0x750010` 的“每次最多 20 只、每条 reply 0x8f=143”已经被真实流量的 `144/67` 请求和 `2862/1289` 回复长度精确印证
    - DLL 侧 `0x1007e4d0 -> 0x1007ef80` 已经把“内部实时对象 -> 对外 `OEM_REPORT(pack=1)`”这层映射压到偏移级
  - 仍然保留为强推断的部分：
    - 还没有拿到“同一份线上 `0x0547` 回包字节”与 `0x100632b0` 内部解析结果的一一对照 dump
    - 因而还不能把“网络侧 `0x0547` payload 的每个字段”逐项、字节级映射到 `OEM_REPORT` 偏移
    - 也还不能把 `7709` 的所有并发主流完全分成“唯一主站 / 辅助分组流”并给每类做职责归因

推断：

- 远端更像私有 TCP 协议，不是直接 HTTP
- DLL 同时内置了本地 HTTP 服务能力，但目前不能证明远端 `6100` 使用 HTTP

## 已做的实测

- 连接 `6100` 成功
- 无 banner
- 裸 `GET / HTTP/1.1` 无回应
- 裸 UTF-8 请求串无回应
- 裸 UTF-16LE 请求串无回应
- `u32 little-endian length + UTF-16LE` 无回应
- TLS 探测失败，不像标准 TLS

## 当前结论

纯 Rust/Linux 方向是可行的，但当前真正卡住的已经不是“外层请求帧是否存在”，而是“登录后的 `penc/字典化` 壳怎么解”。

换句话说：

- 上层语义我们已经拿到
- 返回结构体我们已经拿到
- 远端服务器地址和端口我们已经拿到
- 客户端外层信封已确认是自描述 `网络包` 记录，不是裸 `OEM_DATA_HEAD`
- `6100` 的测速包和 `7100` 的登录包都已经能对到具体对象语义
- 还缺的部分是：
  - `field44 = 12` / `压缩 = ZSTD字典` 的解码
  - `penc` / `Tdx_Encrypt` 的处理
  - 登录后真正行情业务对象的完整解包

## 下一步建议

1. 继续扩展本地 `127.0.0.1:2000` 重组器：在现有按包头 + `declA` 连续消费基础上，进一步补全完整大对象恢复与更多内层类型摘要
2. 对位本地 `2000` 与上游 `6100/7100` 的壳层字段，重点核对 `网络包/penc/hypenc` 的同构位和差异位
3. 把已落地的 `Ask(...)` 统一消息模型继续接到 HTTP/Web UI 调试入口，并在更大样本上补齐 `提示信息 / 消息 / 股票数据 / 错误` 之外的稀有 `form`
4. 直接沿 DLL 逆向 `penc` / `Tdx_Encrypt` 相关分支，而不是继续盲猜抓包字节
5. 优先围绕 `7100` 这组包追：
   - `611` 字节 `认证|登录`
   - `1540` 字节 `数据|下载文件`
   - 以及后续两个 `field44 = 12` 的 `ZSTD字典` 包
6. 继续确认 `系统\\通达信股票服务器.ini` 是登录后下发的网络配置，还是只是升级/测速配置
7. 如果继续抓包，优先保留“本地 `2000` 桥包 + 上游 `6100/7100/7709`”同窗口联动样本，便于跨层校对
8. 当前不建议优先追 `183.236.97.134:14017`，因为它暂时不匹配 `网络包` 头，也没有出现可对齐的中文对象字段

## Linux 侧辅助工具

当前仓库已提供：

```bash
cargo run --example proto_probe -- --help
cargo run --example stream_analyze -- client_to_server.raw --bin
cargo run --example pcap_summary -- capture.pcap
cargo run --example pcap_reassemble -- capture.pcap --src 192.168.3.38:2697 --dst 39.108.103.69:7100 --out /tmp/flow.bin
cargo run --example dll_call_xrefs -- ./netzip_api_bin/NetzipAPI/StockC++/Stock64.dll 0x1800129a0 0x1800128c0
```

可用于继续试验不同发送格式。
